# Python
__pycache__/
*.pyc
.venv/
venv/

# 密钥与环境
.env

# Node / 构建产物
site/node_modules/
site/.vitepress/dist/
site/.vitepress/cache/
site/.vitepress/frag/

# 日志
data/logs/

# OS
Thumbs.db

# 生成层 / 大体积资产
# data/assets/img 是真图字节（唯一不可逆资产），改用 Git LFS 跟踪入库，
#   换机器 clone + git lfs pull 即可拿到图，无需重新下载原站。
# data/parsed、site/public、site/*.md、site/.vitepress/frag 均可由
#   流水线从 data/raw（已入库）+ 源码本地重建，不入库。
data/parsed/
site/public/
site/**/*.md
recycle_bin/
.vscode/
