# 2026-07-30 工作日志

## 表格全屏内角色浮窗触发 + 浮窗层级/Esc 协调（已实现，未构建）
- 需求:页面表格「页面内全屏」时,全屏表格里的角色头像/名字仍要能触发小浮窗(悬停预览)与点击固定大窗;并理顺「全屏表格 / 悬停小窗 / 固定大窗」三者逻辑;「打开完整详情页」改为新标签页打开且点击即关浮窗。
- 根因:`tableEnhancer.openFullscreen` 把 `.escah-tbl` 容器 `document.body.appendChild` 到 `.escah-tbl-fs` 遮罩(移出 root),而 `MirrorContent` 的 `mouseover`/`click` 监听原挂 `root` → 全屏表格内的角色触发全部漏掉。且 z-index:`.escah-tbl-fs`=250 高于 `.char-hover`(60)/`.char-modal-mask`(200)/`.char-modal`(201),会把角色浮窗盖住。
- 改 `MirrorContent.vue`:① `findChar` 向上查找终止条件由 `root.value` 改为 `document.body`;② `mouseover`/`click`×2 监听改挂 `document`(兼容全屏表格),`mouseleave`(onOut)仍挂 `root`(document 上不可靠);onUnmounted 同步改。
- 改 `tableEnhancer.ts`:`import { charModalStore }`;`onFsKey`(Esc)改为「若 `charModalStore.visible` 则返回,不关全屏」→ 角色浮窗优先接管 Esc(因 document 级 keydown 先于 CharHoverModal 的 window 级触发,此时 visible 仍 true);`closeFullscreen` 开头若 `mode==='hover' && visible` 则 `store.close()`(收起悬停预览,固定态保留)。
- 改 `CharHoverModal.vue`:`打开完整详情页` 的 `<a class="fullpage">` 加 `target="_blank" rel="noopener noreferrer"` + `@click="store.close()"`(新标签打开且关闭当前浮窗)。
- 改层级(custom.css / CharHoverModal scoped):`.char-modal-mask` 200→270、`.char-modal` 201→271;`.char-hover` 60→260;均高于 `.escah-tbl-fs`(250)。图片灯箱 `.lightbox-mask` 300 仍最高。
- 处理逻辑总结(自顶向下 z):lightbox(300) > char-modal(271)/mask(270) > char-hover(260) > tbl-fs(250) > VPNav(100)。全屏内 hover→预览(260 在 250 上可见);点击→固定大窗(271)盖全屏;Esc 先关浮窗再关全屏;点遮罩关浮窗、点背景/✕关全屏。
- lint 0 错。**构建未跑(用户拒了 build 命令)**;待用户自行 `cd site && node build.mjs build`。

## llm_reco v0.7:用户提供真实觉醒属性强化表,修正 v0.6 错误常数
- 用户给出游戏真实觉醒表,推翻 Step3 两常数:①行动速度 = -0.1×20次=最多**-2秒扁平**(强化下限3sec,基础≤3s零收益),非旧"+3%除法";②必杀充能 = +0.3×20=最多**+6pt,上限15%**(基础≥15零收益但不削),非旧"+15pt"。用户强调:上限3s/15%对,但强化次数有限非人人可达。另采纳攻/魔+20%(全员同幅进base_hit)。连击率+20(上限50%)不采用(50/70档已达上限;0档必杀型点了interval_ult×1.2反拖慢,理性不点)。
- `_char_score.py` 改 v0.7:新常量 AWK_SPEED_SEC=2/AWK_SPEED_FLOOR=3/AWK_CHARGE=6/AWK_CHARGE_CAP=15/AWK_ATK=0.20 + `awaken_speed()/awaken_charge()`,三处公式(dps_of/拆解表/team_dps_vs)同步。输出 `llm_reco/_score_v07_out.txt`。
- 结果洗牌:maxDPS 7,239→6,181;**幻忍コテツ75.4 登顶反超 レジェンド・ハルカ69.3**(ハルカ基础3.0s已在下限觉醒零收益+连击10%惩罚;コテツ4.0s→3.0s追平+连击0%)>黒門天63.7>ブライド・スバル58.6>エスカ・オニキス57.5。核心洞察:**觉醒下限3s把基础3~5s拉平,"基础速度快"不再是护城河**。連撃型必杀占比再降(ニル13.1%)。四队:D 33,897>C 16,625>A 12,750>B 8,014(结论不变)。
- 文档同步:reco-team.md §5(v0.7)、reasoning-log.md 新增 **§10**、site/{zh,ja}/llm-recommend.md 第九节改 v0.7。**站点未 build**。
- 遗留:装备仍是占位(主装+300/贴片B8·B1·B3),用户默认配装=满级火箭+腰带+纳豆,等用户给三件满级数值+拍板贴片去留(A留/B不留)再替换(reasoning-log §10.4)。→ 已由 v0.8 解决(见下)。

## llm_reco v0.8:用户拍板真实两件配装(副装/纳豆全删)
- 用户决策:副装贴片全删(多变量);仁王纳豆也删(与部分角色技能效果冲突,多变量)。默认**仅两件主装(满级)**:火箭引擎/咆哮猛虎 行动速度-50%、冲击腰带 攻魔+50%。旧占位(主装+300/贴片B8降防30%·B1攻30%·B3速20%)全部作废。
- `_char_score.py` v0.8:常数 EQ_ROCKET_SPEED=0.50/EQ_BELT_ATK=0.50(删MAIN_EQ_ATK/SUB_*)。公式:interval=max(速-2,3)×0.5/(1+自身速buff);red_coeff=1-R×(1-min(1,降防))(降防只剩kit,无30%打底);base_hit=base×1.20×(1+攻buff+0.50)×(1+增伤)×red;crit=1.25常驻(腰带全员带,不影响排名)。模型假设:先觉醒扁平后火箭乘法。输出 `_score_v08_out.txt`。
- 结果:maxDPS 6,595;**ハルカ 重回TOP1**:レジェンド・ハルカ78.9(降防100%)>幻忍コテツ75.4(降防60%但DPS分100)>黒門天73.0>ブライド・スバル61.3>真夏のハルカ55.3。核心洞察:**删30%降防打底后kit自带降防价值暴涨**(100% vs 60%=red 1.00 vs 0.80=25%伤害差改写头名)。火箭-50%全员等比乘法不改排名。四队:D 42,281(raw 46,979,降防100%完全免削减)>C 16,529>A 12,541>B 7,344,D优势较v0.7扩大。
- 文档:reasoning-log 新增 **§11**、reco-team §5 → v0.8(常量/公式/验证/5.4留白)、site/{zh,ja}/llm-recommend.md 第九节 → v0.8。**站点未 build**。

## 修复 想破→破念 等错译:把词表烘焙进 i18n(已提交+推送 9433ecd)
- 现象:`list-ssr/sr/r.html`(角色一览/分类页)表格里"想破"被译成"破念"。用户在 `glossary/high_freq.yaml` `_precise` 段定了 `想破: 想破`(同形词,ja==zh),但渲染期覆盖救不了。
- 根因:`_load_high_freq_glossary` 的 `_HF_ALL_NORM` 只保留 `k!=v`,**同形词被整体跳过**;且子串替换要求 zh 含正确/日文形态,"破念"两样都不含→无从替换。只能在 i18n JSON 源头改。
- 方案:写 `tools/_apply_glossary_to_i18n.py`,**分词对齐**把 `high_freq`+`names`+`skills`(JA→ZH)烘焙进 `data/parsed/i18n/**`(含 characters/)。ja/zh 按「CJK/字母数字段 + 分隔符段」切分,段数相等时把对应 zh 段强制设为词表值;段数不等则跳过(安全,绝不瞎猜)。⚠️**全角标点（）必须归分隔符段**(否则 ja 半角()切4段、zh 全角（）切1段→段数不等跳过,想破修不成——初版踩坑)。
- 结果:416 文件/10646 条改动;全站 i18n "破念" 清零(0 处);抽查 180 条样本全为正确专名统一/残留日文修正(如 閃忍ハルカ→闪忍遥、レガリア→王权、戦士(現界)→战士（现界）)。少数看似乱的 zh 是**原 i18n 既有坏译文**,非本脚本引入(脚本只写中文词表值,绝不写日文)。
- 已 commit `9433ecd` 并 push `61b6007..9433ecd`。CI 构建直接读已提交的 i18n JSON(不跑 i18n build/extract/fill),烘焙结果持久生效;本地若跑 `i18n build` 会覆盖,须重跑此脚本。

## 修复 正文 `#id` 锚点跳转失效(已提交)
- 现象:`main-quest.html` 等页面正文里 `<a href="#drop_list">掉落物品一览</a>` 跳转不生效,其它页同理。
- 根因:`sitegen._strip_nav_links` 对 PukiWiki 的 `anchor_super`(`<a id="drop_list">†</a>`)直接 `drop_tree()`,而这些 `<a>` 正是**正文 `#id` 链接的真实跳转目标**;目标被删→`MirrorContent.onAnchorClick` 的 `getElementById` 返回 null→默认跳转也无效。所有 i18n 页(JA/ZH 都走 `render_locale`→模板)在 `i18n build` 时经 `_sanitize_html` 把 `anchor_super` 连同 `id` 丢了。
- 修复:`_strip_nav_links` 丢弃 `anchor_super` 时若带 `id`,改为保留占位 `<span id="...">` 落回原父元素内(去 † 留锚点)。改后重跑 `i18n build` → 417 个 `*.template.html` 补回 span(`<span id="drop_list">` 等)。
- ⚠️ **`i18n build` 副作用**:顺带用当前 names/skills.yaml 改写 31 条专名读音(如 古纳冈→古纳甘/乌塔尔→乌尔塔尔/弗栗多→弗利特拉 多为正确纠正,但混入劣化 `歼忍 影`→`歼忍歼忍影影`、`迪斯特邦`→`Distburn` 裸罗马音)。因本次只修锚点、不动译文,**把这些 JSON 改动 `git checkout` 还原**,仅留模板补丁 + `sitegen.py` 源码修复。
- 提交内容:`pipeline/escah_pipeline/sitegen.py` + `data/parsed/i18n/**/*.template.html`(417)+记忆文件。推送后 CI 经 sync-site 重建 frag(锚点 span 进入渲染),部署生效。

## 烘焙脚本补强:规则0 整条ja精确匹配(用户指正,待确认执行)
- 用户指正:词表烘焙本应「以 ja 为键匹配 i18n 的 ja,直接替换 zh」,不该搜中文。澄清:搜中文的是渲染期 `_correct_text`/`_CORR_RE`(只拿得到 zh 字符串的事后补丁层,结构性救不了错译);烘焙脚本本来就是 ja 驱动,但缺「整条 ja==词表键→zh 直设词表值」规则,全靠分词对齐(段数不等跳过)。
- 已改 `tools/_apply_glossary_to_i18n.py`:新增**规则0**(最高优先):整条 ja 精确命中 gloss(含 strip/去空白 gloss_ns 容错)→ 直接 return 词表值,不看 zh 现值、无需对齐;原分词对齐降为规则1(处理键为长句子串场景)。
- `--dry` 结果:408 文件/3911 条 zh 会被修正(如 超昂フェス→超昂盛典、技能名/固有效果整条对齐词表)。这些当前靠渲染期 `_name_override` 兜底显示正确,烘焙后源头即正确。样本:`tools/_logs/apply_glossary_i18n.log`,dry 输出 `tools/_logs/_gloss_dry.txt`。
- **已执行+提交+推送(用户指令)**:实际写入 408 文件/3911 条(顶层48+characters/369),commit `fbdb94c`,push `41dd730..fbdb94c main`。CI 重建 zh 站读已提交 i18n JSON,源头即正确。
- **澄清"build 会错误覆盖"是误导(用户逼问后更正)**:`build_page`(i18n.py 817-818)在 JSON 阶段**不套词表**(专名替换推迟 render_locale),只做「记忆回贴」`zh=memory[norm(ja)]`,memory 取自跑之前该 i18n json 自身。故先烘焙后 build:build 读已修正 zh 回贴,修正原样保住。唯一差异:分段 ja 切分粒度变了→memory 查不到→`zh=""`(漏译非错译)。旧笔记"build 套 names/skills 产生 31 条漂移(歼忍 影→歼忍歼忍影影)"对当前代码不成立(已据代码更正 MEMORY.md)。实践仍建议 build 在前烘焙在后保证覆盖率。
