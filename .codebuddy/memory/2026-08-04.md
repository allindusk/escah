# 2026-08-04 工作日志

## raid 表第一列 BOSS 链接丢失（i18n 块级回退 bug，已修复）
- 现象：`raid.html#content_1_23`（各团战BOSS特性表）第一列 BOSS 名原日文是 `<a href="raid-00X.html">`，中文站却全变纯文本（仅"狂王"因 link_terms 配置了链接而保留）。
- 根因：`render_locale` 块级回退分支对"含 `<a>` 且块级译文完整的块"调用 `_fill_block_keep_links`，旧实现**整块清空子节点塞纯文本 → 丢失 `<a>` 的 href**；又依赖末尾 `_apply_config_links`（link_terms）重包，但 raid 表 BOSS 链接未配 link_terms，故整列消失。
- 修复（`pipeline/escah_pipeline/i18n.py`，未提交）：
  1. `_fill_block_keep_links` 重写为**保留原 `<a>` 结构**（href/title 不动），仅用 `_sub` 把块内文本节点/`el.text`/`tail` 里的 `{{keyN}}` 替换为中文。
  2. 块级回退分支（~1519）判断 `has_a` → 有链接走保留版，无链接才整块纯文本退化。
  3. `_sub` 闭包提前到 `_BLK` 分支前定义，删除原 1617 重复定义。
- 验证：`node build.mjs build` 后 `dist/zh/raid.html` 503-509 行 7 个 BOSS（黄金哈尼/古纳冈/乌塔尔/堕威大佛/大往女/狂王/弗栗多）均恢复 `<a class="internal-link" href="raid-00X.html">`，`(第N回～)` 文本保留在链接外；link_terms 未重复包狂王。preview 已重启（端口 4173）。
- 关联：MEMORY.md「词表注入逻辑」节已同步修正（旧笔记"按长度切分"描述错误，实际旧版是整块纯文本丢链接）。

## limit-break 列表译文丢失（续修块级回退，已修复）
- 现象：`limit-break.html` 完成突破优势三条 `<li>`（blk5/6/7，含 `<a>` 链接 battle/equipment/skills）中文站显示**日文原文**（ステータス/装備スロット）或空 `<li></li>`。用户铁律：节点级/块级有译文就显示译文，绝不该回退日文；无相对词汇译文才写待办 link_terms 精修。
- 根因：上一版 `_fill_block_keep_links` 只做节点级 `_sub` 替换（空 zh → 回退日文），**完全没用块级译文 blk_zh**；且单 `<a>` 分支错误地移除了 `<li>`/`<strong>` 父节点 → 整块变空。
- 修复（`pipeline/escah_pipeline/i18n.py`，未提交）：
  1. 函数内先判断块内 `{{keyN}}` 节点级译文是否齐备：齐备→逐节点 `_sub`（保留各自译文+链接）；**不齐备→用块级译文 `blk_zh`，绝不回退日文**。
  2. 不齐备时：块内单 `<a>` → 保留 `<a>` 壳及祖先结构、`blk_zh` 进 `<a>.text`、清 `a.tail`（链接零丢失）；多 `<a>`/复杂 → 整块纯文本 `blk_zh`（链接丢失但显示中文）。
  3. 修正单 `<a>` 分支**不再移除父节点**（否则空 li）。
- ⚠️ 关键教训：`render_locale` 在 **sync-site 阶段**运行生成 frag，build.mjs 只打包。改 i18n.py 后必须 `sync-site` + build，只 build 会用旧 frag（首轮修复 dist 显示空 li 即因漏跑 sync-site）。
- 验证：dist 三条 li = `<a href="battle.html#ed5f3d74">角色基础属性大幅提升，最高3倍增幅</a>` / `<a href="equipment.html">解锁装备槽位</a>` / `解锁必杀、固有效果强化功能（实际强化需依靠觉醒强化系统）`；日文残留 0；raid 表 52 处 BOSS 链接无回归。preview 重启（进程 5180，4173）。

## faq blk4 整段中文被塞进单个超链接（`_fill_block_keep_links` 回归，已修复）
- 现象：用户实测 `zh/faq.html` 第 4 段整句「完全没有问题…顺利推进。」被包成一个 `<a href="bedroom-scenes.html">`，连「观看H场景」也混在里面。link_terms 配置没生效。
- 根因：blk4 有节点级 keys（key205–210），其中 key208/209/210 的 `zh` **漏译为空**。旧 `_fill_block_keep_links` 在"节点级不齐备 + 块含单 `<a>`"时走 `a.text = blk_zh` 分支 → 整段中文塞进单个 `<a>`，且漏译的 key209 没显示中文「观看H场景」。
- 修复（`pipeline/escah_pipeline/i18n.py` 第 1467 行）：块级回退里**凡 `blk` 有 `keys` 一律走逐节点 `_KEY_RE.sub(sub)` 替换**（缺译 key 经 `sub` 回退日文，安全兜底），不再走整块塞 `<a>` 分支。配套：回填 faq.json 的 key208/209/210 节点级 zh（key209=「观看H场景」）；link_terms faq 段 `エッチシーンを見る` 的 zh 由日文原文改中文「观看H场景」以正确命中。
- ⚠️ 验证教训：无头 Edge dump 经**旧 preview 进程**会出现 internal-link 误报 0 的加载时序/缓存假象；应以 **dist 产物**（`site/.vitepress/dist/zh/faq.html`）为准——修复后 dist 实测 `internal-link=333`、`观看H场景=4`、`bedroom-scenes=7`、正文 blk4 正常拆分为中文文本 + 单 `<a>观看H场景</a>`。preview 已重启（4173）。
- ⚠️ 铁律已写入 MEMORY.md：`_fill_block_keep_links` 块有 `keys` 必走逐节点替换，绝不整块塞 `<a>`；漏译节点级 key 须回填 i18n JSON 真值，不能只靠块级整句。

## faq「日文混中文+整段塞 a」真正根因与修复（用户第二次怒，推翻上一修）
- 用户第二次怒：「页面上好多链接词是日文」。查根因：**不是渲染层逐节点 bug，而是 i18n 数据层**——`faq.json` 有 **600 个节点级 key 的 zh 漏译为""**（整块回填 blk.zh 有中文整句，但拆出的 keyN 没回填）。渲染时块级回退对「有 keys 且缺译」的块，旧逻辑（1579 行 `if not all(keys 有 zh)` 走 `_fill_block_keep_links`）把 blk_zh 整段塞进单个 `<a>`（之前铁律那条），而我上一修的「逐节点替换」则让缺译 key 回退日文碎片 → 「日文混中文」。两条都错。
- **正确修复**（`i18n.py` 1467 附近）：块有 `keys` 时**整块替换为纯文本 blk_zh**（权威中文整句），交给 `_apply_config_links` 用 link_terms 中文词精确匹配 blk_zh 重新套链接。无 keys 的纯整块块才走单/多 `<a>` 分支。节点级全齐备的块（blk4 key208/209/210 已回填中文）走原 `continue` 节点级路径（`<a>` 壳保留）。
- 验证 dist：faq 段 `宝箱の開封`/`や「D2P」ガチャ`/`はポイントが` 等日文碎片 = 0，`装备 会从 打开宝箱 以及"D2P"招募中随机产出` 纯中文 ✓。但 `internal-link` 从 333 降到 93（很多块改用 blk.zh 纯文本，链接壳暂丢，需 link_terms 配置才能套回）。
- **走用户定的流程补全链接**：跑 `tools/_extract_link_todo.py` → 归并真值 1340 条 + todo 221 条（reason=zh_japanese，即 link_terms 的 zh 还是日文原文、在中文译文匹配不上）。这 221 条需逐条精修（把 link_terms 的 zh 改成中文译文实际词）才能把链接套回。MEMORY.md 那条铁律已修正。
- 预览重启（4173）。改动文件：`pipeline/escah_pipeline/i18n.py`（渲染修复）、`data/parsed/i18n/faq.json`（blk4 key 回填）、`glossary/link_terms.yaml`（エッチシーンを見る→观看H场景 + extract 归并 1340 条）、`glossary/link_terms.todo.yaml`（新 221 条）。

## 全站排查 + 逐条精修 link_terms todo 221 条（2026-08-04 续）
- 用户要求：不止 faq，全站检查同款 bug；逐条读 link_terms.todo.yaml 的 221 条 zh_japanese，结合 glossary 精翻词 + 中文译文把 zh 改成中文，套回链接。
- 全站漏译 key 扫描（`tools/_scan_empty_zh_keys.py`）：全站 i18n JSON **节点级 key 的 zh 为空共 35331 个**（faq 的 600 只是冰山一角）。但渲染层已修（块有 keys 用 blk.zh 纯文本），所以日文混中文 bug 已全局消失；**链接壳缺失**是剩余问题（link_terms 的 zh 还是日文 → 匹配不上中文 blk.zh）。
- 逐条精修做法（`tools/_fix_todo_links.py` 平行句对齐 + `tools/_patch_link_terms.py` glossary 覆盖 + `tools/_patch_remaining.py` 手工映射，最终落到 `glossary/link_terms.yaml`，原 todo 已被 `_merge_link_terms.py` 合并删除；backup 在 `tools/_todo_backup_before_merge.yaml`）：
  1. 优先用 glossary 四份词表（render-time 覆盖）把 ja 翻译成**渲染后中文**（如 スタミナ→体力、超昂金 パルシオン(攻撃)→超昂金 帕尔西翁(攻击)），写回 zh。
  2. B宇宙专名/无 glossary 映射的：从 b-universe.json 的 key.zh 提取中文（轨道弗拉斯特（B宇宙）等）+ 手工映射补齐。
  3. 验证脚本 `tools/_verify_links.py`（查 dist 中文页该 href 是否被 `<a>` 包裹）。
- 顺带修 2 个渲染层 bug：
  - `_NAV_TAGS` 去掉 `li/ul/ol`（只留 nav/header/footer/aside/menu）：原来把**正文列表内 `<a>`（如装备推荐、B宇宙列表）误当导航过滤**，导致大量列表内链接不套。修后多解决 15 条。
  - `_fill_block_keep_links` 块有 keys 且含**外链 `<a>`**（href 以 http 开头）→ 保留外链壳（内文日文专名/站名），避免纯文本化丢外链。
- 最终结果：221 条 todo → **189 条链接已套回**（verify 套上 189），**剩余 25 条 MISS**：
  - 全部是 **raid-recommended / equip-XXX / exchange 的装备专名与外链**。
  - 根因（非 zh 问题）：这些日文链接词**在对应 i18n 译文 JSON 里根本不存在**（grep `パルシオン`/`DMM公式サイト` 在 raid-recommended.json/exchange.json = 0），即这些块**未被 i18n 解析收录**（漏译或模板静态内容），渲染时整块丢失 `<a>` 壳，改 link_terms 的 zh 无法套回。
  - 装备类译文实际中文是翻译者写的「超昂金装/超昂金体力」等非标准译法（与 glossary 标准「帕尔西翁」不一致），用 glossary 标准 zh 匹配不到；用「超昂金装」又歧义（多装备共用前缀，会误套同一 href）。
  - **解决路径**：需给 raid-recommended/equip/exchange 补 i18n 译文（把这些装备名/外链文本翻译并收录进 JSON），或用户逐条定译名后回填——非单纯改 zh 能解决。已向用户汇报待决策。
- 验证以 dist 为准；改动未提交（用户未指令 push）。

## 剩余 25→4 条 MISS 的精确根因与选 A 修复（2026-08-04 傍晚）
- 用户决策：**A = 补 i18n 译文**（给缺译 key 填中文/日文）。
- **推翻上一节"i18n 里不存在"的错误结论**：实际这些日文链接词**都在 i18n JSON 里**（grep `パルシオン`/`DMM公式サイト` 在 raid-recommended.json=2、exchange.json=0 例外）。真正根因是**块级 keys 缺译**：这些 `<a>` 外层有 `data-i18n-blk="blkXX"`，块的 keys 列表里**某个 key 的 zh 为空**（如 blk67 的 key391=`および`、blk59 的 key339=`で。`、blk110 的 key638=`ベルトと` 等句尾助词/连接词，或 blk82 的 key469=`【キャラ開放ストーリー】` 等正文片段）。→ 节点级不齐全 → 渲染走 `_fill_block_keep_links` 的"块有 keys 分支"（i18n.py ~1467）→ **删除块内所有子元素（含 `<a>`）纯文本化 → `<a>` 壳丢失** → 链接 MISS。
- 修复（`tools/_patch_miss_i18n2/3/4.py`，落 `data/parsed/i18n/*.json`，未提交）：给所有缺译 key 补 zh（连接词/句尾助词→中文等价「以及/与/。」；正文片段→中日同形保留日文），使块级 keys 全齐 → 渲染 line 1590 `all(keys zh)` 为 True → `continue` 保留 `<a>` 壳 → 链接套回。
- 结果：verify 从 **31 条 MISS → 4 条 MISS**（27 条全修复）。类型1（外层有 blk 但 keys 缺译，如 b-universe/equip-089/095/character-exchange 部分/equip-013/018/028/buniv-001）+ 类型2（纯 `<a>` 块，key 已填中文但外层 blk keys 缺译，如 raid-recommended 8 条/character-exchange 3 条/gacha）全部修复。
- **剩余 4 条 MISS 全是 exchange 的 DMM 外链**（`DMM公式サイト`/`月間ブログ（26年5月20日）`/`DMMプリペイドカード`/`DMMポイントチャージ`，href=dmm.com/download、alicesoft/entry003948、payment.dmm、point.dmm）。核查：`data/parsed/ja/exchange.html`、`exchange.template.html`、`dist/zh+ja/exchange.html` 三处 **dmm.com 全部 = 0**——这些外链在**当前站点数据里根本不存在**（孤儿/过期条目，源站已移除或 parse 漏收）。**补 i18n 译文无解**（源无对应块），只能：(1) 从 `glossary/link_terms.yaml` 移除这 4 条（避免 verify 误报）；或 (2) 保留待源站补内容。已向用户汇报待决策，未擅动。
- 教训：verify 报"链接 MISS"的**第一因不是 rewrite 层，而是 i18n JSON 块级 keys 缺译导致渲染纯文本化丢 `<a>`**；诊断须先看「该 `<a>` 外层 blk 的 keys 是否全齐」而非先 grep 词是否存在。
- 改动文件：`data/parsed/i18n/{b-universe,raid-recommended,equip-089,equip-095,character-exchange,equip-013,equip-018,equip-028,buniv-001,gacha}.json`（补缺译 key）、`pipeline/escah_pipeline/i18n.py`（上一节已修渲染层）。未提交、未 push（用户未指令）。

## DMM 链接词 zh 译名修正（2026-08-04 晚，续上）
- 用户给出 4 组日中对照：`DMM公式サイト`→`DMM官方网站`、`月間ブログ（26年5月20日分）`→`月度博客（26年5月20日分）`、`DMMプリペイドカード`→`DMM预付卡`、`DMMポイントチャージ`→`DMM点数充值`。
- 已落 `glossary/link_terms.yaml`：4 类 × 3 处（exchange 段 + getting-started 段两子段）= 12 条，全量校验 0 问题。只改 `zh` 行，`ja` 键不动（渲染期用 ja 匹配译文）。
- 修复中发现的**原文件损坏/错字**（非本次引入）：① exchange 段 `月間ブログ` 的 zh 原为乱码 `月間博客人（26年5月20日分钟）`（「博客」→「博客人」、「分」→「分钟」）；② `DMMポイントチャージ` 的 zh 原为错字 `DMM点数充能`（应为「充值」）。均改对。
- ⚠️ **工具行号偏差陷阱**：`read_file`/`search_content` 显示的行号比文件实际行号（Python 0-based）**多约 2 行**（疑似 CRLF 计数差异）。诊断"某行没改到"时务必用 Python 按实际索引读取，勿信工具行号。
- 注：这些 DMM 外链在源站 exchange 页已不存在（dmm.com=0），故改动当前页面无视觉变化，但配置已正确，源站若恢复链接即生效。sync-site+build 验证通过（exit=0）。
- 用户下一步待决策：是否从 `tools/_todo_decided.yaml` 移除这 4 条孤儿（让 verify 收敛到 0），`link_terms.yaml` 真值不动。未 commit/push。

## 链接套用改为「块级锚定」，杜绝强加链接（2026-08-04 晚，重要）
- 用户原则：**原日文没有超链接的地方，中文页面绝不能加超链接**；用 `glossary/link_terms.yaml` 必须「锚定页面 + 文本具体位置」再处理。
- 根因诊断（对比实验）：旧整页级 `_apply_config_links(html, ja_link_words)` 做「全文词级子串匹配」——只要 link_terms 的 `ja` 在某页原文是链接，就在中文页该词**所有出现位置**套 `<a>`，导致原文同词「A处链接/B处纯文本」时 B 处被强加。且因 `ja_link_words` 收集原文 `<a>` 完整文本、而配置 `ja` 多为子串，精确匹配下整函数空转（0 强加也 0 生效）。
- 修复（已落地 `pipeline/escah_pipeline/i18n.py`）：
  1. 新增 `_wrap_block_links(blk_zh, src_links)`：在 blk_zh 纯文本里，**仅为「原块内真实存在的 `<a>`」**（src_links=收集到的 (href, 日文链接文本)）补回 `<a>`；每个原 `<a>` 只包一次、长词优先防嵌套。译文优先查 link_terms→glossary→原文(中日同形)。位置一一对应原文 `<a>`，**绝不跨位置强加**。
  2. `_fill_block_keep_links`「块有 keys」分支：纯文本化为 blk_zh 前先收集块内内链 `<a>`(外链走原特例)，纯文本化后用 `_wrap_block_links` 块级锚定补回。取代原「整块丢链接等整页套」。
  3. 整页级 `_apply_config_links` 调用点**删除**（改注释说明由块级接管）。`_ja_link_words_for_slug` 重命名为 `_ja_link_pairs_for_slug` 返回 (文本,href) 对（为后续整页兜底预留，当前未用）。
- 验证：`node build.mjs build` 通过（exit=0）；禁用对比实验确认旧整页函数零效果，新块级方案结构保证「只有原文有 `<a>` 的位置才有中文链接」。preview 已起重启（PID 5792, http://localhost:4173/esah/）供用户检查。未 commit/push。

## ⚠️ 用户反馈「faq#content_1_107 宝箱被强加链接」根因与修复（2026-08-04 晚，重要）
- 用户贴：日文原文 `宝箱` 无 `<a>`，但中文页 `宝箱` 带 `<a href="/escah/zh/treasure-box.html">`。
- 排查（用临时 trace + 多轮比对，最终定位）：
  - i18n render 层 `_wrap_block_links` / `_sub` 全程 trace 0 命中 → 不是 render 强加。
  - `data/parsed/i18n/faq.json` 的 key1332.zh 纯文本无 `<a>`；`_correct_text`(`_name_override`/`_term_override`)对「宝箱」均返回纯文本，绝不包 `<a>`。
  - **真正根因 = 过时产物**：`site/zh/faq.md`（sync-site 生成）是旧的，含 `宝箱` `<a>`；而 `data/parsed/i18n/faq.template.html` 当前已无 `<a>`。我之前只 `i18n build` + `node build`，**没重跑 `sync-site`**，导致 faq.md 停留在旧状态，build 从旧 faq.md 产出含链接的 dist。
  - 重跑 `python -m escah_pipeline.cli sync-site` + `node build.mjs build` 后，最新 dist 的 content_1_107 段 `宝箱` `<a>` = **False**（消失），符合「原日文无链接中文不加」原则。
- ⚠️ **铁律（最高优先级）**：改 i18n 渲染逻辑 / 模板 / link_terms 后，**必须 `sync-site` + `build` 完整重跑**才能验证。只 build 不 sync-site 会看到过时产物（faq.md 是 sync-site 产物，非 build 产物）。本次耗十几轮才定位，根因就是漏跑 sync-site。
- ⚠️ 本地 `data/parsed/ja/faq.html` 快照里 content_1_95 等段的 `宝箱` **确实有** `<a href="treasure-box.html">`（PukiWiki `[[宝箱]]` 内联链接解析结果），那些保留链接是忠于本地源的、正确，不应删。用户看到的「无链接」是线上最新版（本地快照偏旧），非渲染 bug。
- 已清理所有临时 trace 代码（i18n.py 的 WRAP_TRACE/SUB1332 print）和 ~40 个临时探测脚本/日志。preview 重启 PID 18984。未 commit/push。

## b-universe BOSS 超链接丢失 + HTML 转义 bug（2026-08-04 夜，已修复并验证）
- 现象：用户实测 `zh/b-universe.html#content_1_10` 的「攻略详情请参考 波涛弗拉斯托（B宇宙）。」**无 `<a>` 超链接**（日文镜像/源站这些 BOSS 都是有 buniv-00X.html 跳转的）。
- 根因双 bug：
  1. **全局索引被垃圾条目覆盖**：`glossary/link_terms.yaml` 的 `characters` 段（5235 行附近）有一条重复 `ウェーブフラスト（Bユニバース）`→`zh: 波浪弗拉斯特（B Universe）/' 下没有子页面。` 的损坏条目，覆盖了全局 `_LINK_JA_ZH` 索引（后写覆盖先写）→ 渲染时查到的中文在 blk_zh 里匹配不到 → 链接丢失。**已删除该垃圾条目**。
  2. **HTML 标签被实体转义**：`_fill_block_keep_links` 两处用 `el.text = _wrap_block_links(...)` 直接赋含 `<a>` 的 HTML 串，lxml 把 `<` 转义成 `&lt;a&gt;` → 浏览器显示成纯文本 `<a ...>` 而非可点击链接。**已新增 `_set_block_html(el, html_str)` 用 `lxml_html.fragments_fromstring` 解析后挂入 el，替换两处 `el.text =` 赋值**。
- 增强 `_wrap_block_links` 鲁棒性：全局索引命中但其中文在 blk_zh 里找不到时，回退 glossary 覆盖/原文，避免单条坏数据毁整页。
- 验证（以 `dist/zh/b-universe.html` 为准）：全部 12 个 BOSS 的 buniv-00X.html 链接均恢复可点击。其中 5 条「攻略详情请参考」类已全中文（波涛弗拉斯托/科学弗拉斯托/灭忍菊理＆铁/迪斯特邦/奥尔特纳四天王）；另 5 条「攻略详情参见」类（轨道/影像/歼忍铁/艾斯卡蕾雅/奥尔巴）专名已音译、但括号仍「ユニバース」未统一成「宇宙」——属翻译真值层（专名已翻，括号未统一），链接跳转正常，待用户决策是否统一。
- 改动：`pipeline/escah_pipeline/i18n.py`（_set_block_html + 转义修复 + 鲁棒性）、`glossary/link_terms.yaml`（删 characters 段垃圾条目）。未 commit/push。preview 仍跑旧进程（4173 占用中，已服务最新 dist）。

## B宇宙 BOSS 链接词「Bユニバース→B宇宙」统一（2026-08-04 夜，已修复并验证）
- 用户指出之前叫过「根据词汇表翻译」，但 5 条「攻略详情参见」类 BOSS（轨道/影像/歼忍铁/艾斯卡蕾雅/奥尔巴）的专名已音译、括号却仍是日文「（Bユニバース）」未翻成「（B宇宙）」。
- 根因（迭代定位）：`_name_override` 用 `_split_name_suffix(ja)` 把「（Bユニバース）」当 **suffix** 切出，base=「レールフラスト」命中 names.yaml→返回「轨道弗拉斯托」+suffix「（Bユニバース）」=「轨道弗拉斯托（Bユニバース）」，且该覆盖优先级最高（_sub 第 1711 行 return）直接盖掉正确的 key.zh「轨道弗拉斯托（B宇宙）」。
- 修复三处叠加：① `terms.yaml` 的 `inline_terms` 段加 `"Bユニバース": "B宇宙"`（含假名→进 `_TERM_SUB_MAP` 子串层）；② `_wrap_block_links` 返回前套 `_term_sub_override`（整段）；③ **关键**：`_name_override` 的 suffix 拼接返回值（第 404/406/409 行）套 `_term_sub_override`，统一后缀里的「Bユニバース」→「B宇宙」。
- 验证（dist/zh/b-universe.html）：全部 12 个 BOSS 的 buniv-00X.html 链接均可点击且文本全中文「（B宇宙）」。残留：6 处正文内 `<a title="...（Bユニバース）">` 的 title 属性仍是日文原词（显示文本已中文），作为悬停 tooltip 保留日文，未改（符合专名保留日文惯例）。
- 改动：`glossary/terms.yaml`（inline_terms 加 Bユニバース→B宇宙）、`pipeline/escah_pipeline/i18n.py`（_name_override suffix 套 _term_sub_override + _wrap_block_links 末尾套 + 单/多链接分支 blk_zh 套）。未 commit/push。
