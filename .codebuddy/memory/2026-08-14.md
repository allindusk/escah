# 2026-08-14

## official-help 页面：加目录 + 删ライセンス 模块
- **需求**：①给 official-help 加页内目录(TOC)；②删除页末「ライセンス」模块(第三方著作物/字体商标声明，原 key960-963 + blk523，含 UniRx/MiniJSON/DOTween/DynaFont 等 MIT 许可全文)不展示。
- **实现**（pipeline/escah_pipeline/i18n.py）：
  1. 新增 `_slugify()` + `_inject_toc(html, locale, slug)`：仅 `slug=="official-help"` 生效，收集所有顶级 `h2`（排除 `.help-entry` 小标题），给每标题加稳定 id（`_slugify`，中日文安全），在第一个 h2 前注入 `<div class="contents"><p>目录/目次</p><ul>…</ul></div>`。目录标题 zh=「目录」/ ja=「目次」。
  2. 在 `render_locale(slug, locale)` 末尾 `return html` 前调用 `_inject_toc(html, locale, slug)`。
  3. **持久删除ライセンス**：在 `build_page()` 里 `slug=="official-help"` 时，从 frag 删掉文本为「ライセンス」的 h2 及其后续所有兄弟（因它是页末最后一块）。这样 `i18n build` 不会重生该块，删改对源 raw 重 parse 也健壮。
- **关键坑**：`i18n build` 从 `data/parsed/ja/official-help.html`(源 raw 重生成) 重建 template+JSON，所以**手动删 template/JSON 会被 build 复活**；必须在 build_page 渲染期删，而非改 template/JSON。
- **验证**：`i18n build --pages official-help` → `sync-site` → `build`。dist/zh 与 dist/ja 均含 `.contents` 目录(各30章)，`ライセンス|UniRx|DynaFont` 0 匹配。标题 id 与 TOC 锚点一致，跳转可用。
- 未 push（无用户指令）。

## 项目精简（0 风险清理，全部进 recycle_bin/ 可回退）
- **用户诉求**：项目臃肿，要求 0 风险精简。澄清了 `data/parsed/zh/` 与 `ja/` 数量不一致是设计（zh 旧渲染层副产物、不入库不被读，线上页真值来自 `i18n/`+`dist/`），并确认官方镜像页来自 `i18n/`→`sync-site`→`frag`→`build`，非 `ja/zh` 文件夹。
- **实际迁移（git rm --cached 取消跟踪 + 物理移入 recycle_bin/tools_20260814/）**：`tools/_docx_out/`(418 docx)、`tools/_translated_texts/`(461)、`tools/_todo_translate/`(11)、`tools/_merged_chars/`(5)、`tools/_logs/`(98)。这些是被 `.gitignore` 漏掉而误入库的**翻译/导出中间产物**（真值已落在 `data/parsed/i18n/`，txt/docx 是过程文件）。
- **根日志**：`site/_build_out.txt`/`_preview*.log`/`build.err`/`dev.err` 及根 `_*.txt`(9个) 移入 recycle_bin/root_20260814/（本地未入库，纯清理）。
- **保留**：`_chk_*`/`_dbg_*`/`_dl_*`/`_list_*`/`_verify_*` 调试/抓取脚本（工具非垃圾）；真值 `data/parsed/i18n`(1326)/`glossary`(5)/`pipeline`(22) 完好。
- **误判修正**：`data/parsed/zh/` 经 `Get-ChildItem` 核实**磁盘上本就不存在**（list_dir 早前返回的是 microcompact 折叠的陈旧缓存结果），故无实际删除。
- **.gitignore 补规则**：新增忽略 `tools/_docx_out/ _translated_texts/ _todo_translate/ _merged_chars/ _texts_for_translation/ tools/_*.json`，防再次误入库。
- **效果**：git 跟踪文件 5444 → 4556（减 888）。未 push（无用户指令）。
- ⚠️ 经验：本项目 list_dir 对大目录可能返回折叠缓存，涉及磁盘真实存在性务必用 `Get-ChildItem`/`Test-Path` 二次核实。

## official-help TOC 两级 + 底部原网页链接修复
- **TOC 改两级**：用户贴了原站官方帮助目录结构（约 20 个 L1 章节 + 90+ 个 L2 子条目，help-entry 小标题挂在最近 L1 下）。之前版本误把 h3/h4 当 L3 导致层级错乱。重写 `_inject_toc`：只取 `<h2>`，`class=help-entry`=L2 挂最近 L1，不含 h3/h4；ライセンス 模块已在 build_page 删除故自动不入目录。CSS 两级区分：L1 紫粗+左边框条，L2 缩进+`›`前缀更小更浅。
- **关键澄清（用户误以为"解析缺失导致翻译缺失"）**：实际 official-help.json 921/960 块已有 zh，正文（含あらすじ 永遠に続く…战部時定…整段）<u>翻译完整、解析无缺失</u>。真正缺译的是 **39 个 help-entry 小节标题**（zh 为空的 key，如 ホーム/資金/ストーリー/クエスト/お知らせ/ミッション/プレゼント/ステータス/情報/装備/アイテム 等），其中仅 蝶鉱石→蝶矿石、宝箱→宝箱 被 glossary 覆盖，其余 37 条生成待译文件 `tools/_todo_translate/official-help_todo_20260814.txt`（生成器 `tools/_gen_official_todo.py`）。
- **底部原网页链接改 e-heroines**：用户要求改为 `https://prod.e-heroines.net/server/product/AppSub#`。根因：sitegen 的 sourceUrl 来自 `Manifest()`（抓取 JSON）而非 pages.yaml，故仅改 pages.yaml `url` 无效。已修改 sitegen L507：`source_url` 优先级 = registry entry 的 `url` > manifest url > SOURCE_BASE 兜底；并在 `data/registry/pages.yaml` 的 official-help 条目加 `url:` 字段。已验证 zh/ja dist 底部链接均更新。
- ⚠️ 铁律补充：改某页"原网页链接(sourceUrl)"须在 `data/registry/pages.yaml` 该条目加 `url:` 字段，并依赖 sitegen 的 registry 优先逻辑（Manifest 不读 pages.yaml）。

## 纠正：回归标准待译流程（勿自创格式/旁路脚本）
- **用户批评**：我用 `<key>\t<日文>\t<中文>` 自创格式 + `_gen_official_todo.py`/`fill_official_todo.py` 两套旁路脚本生成 official-help 待译，**完全违背项目既定流程**。项目标准待译格式由 `i18n extract` 产出：`tools/_todo_translate/new_translation_<YYYYMMDD>.txt`，条目 `[N] 日文`，页间 `===A===`/`===B===` 分隔，`# MAP A=<slug>` 记映射；回填走 `i18n fill`（取 `<日期>_translated.txt` 的 `[N] 中文`）。
- **纠正动作**：① 删除旁路脚本 `_gen_official_todo.py`/`fill_official_todo.py` 及误生成的 `official-help_todo_20260814.txt`；② 把"排除 UI 单词碎片"过滤加进标准 `_should_skip_extract()`（新增模块级 `_SENT` 常量 + `_is_ui_fragment()`：以 `・` 开头的列表项、无句末标点且 len≤10 的短名词属性词跳过），这是 extract 统一跳过入口，改动对全站 extract 生效；③ 改回用 `python -m escah_pipeline.cli i18n extract --pages official-help` 生成标准清单 → `new_translation_20260814.txt`（1页 486 条完整句子，碎片已排除，句子内 `[ストーリー]→[メイン]` 等保留）。
- ⚠️ 铁律（用户重申）：**待译文件固定格式与生成/回填流程由 `i18n extract`/`i18n fill` 管控，禁止另写旁路脚本或自创文件格式**。任何翻译策略调整（如排除某类碎片）须改 `i18n.py` 的 extract 逻辑（如 `_should_skip_extract`），而非另起脚本。
- **遗留待用户定夺**：被排除的 UI 碎片（ストーリー/クエスト/ステータス/ミッション/プレゼント/お知らせ/イベント 等独立名词标签）多数**不在 glossary**，渲染期会回退日文。用户策略"靠 glossary+上下文自动处理"与实际缺失存在落差，须用户决定是否补 `glossary/terms.yaml`（专名译名需用户确认，AI 不擅自拍板）。

## official-help 基础词进待译 + 固化 glossary
- **用户决定**：高频基础词（名词性碎片）既要补进 `glossary/terms.yaml`，也要进待译清单由用户亲自翻译（AI 不擅自定译名）。
- **实现**：给 `_should_skip_extract(ja, allow_ui_fragments=False)` 和 `_untranslated_items(slug, allow_ui_fragments=False)` 加参数；`extract_todo` 对 `slug=="official-help"` 传 `allow_ui_fragments=True`，使 UI 碎片词进清单。重跑 `i18n extract --pages official-help` → `new_translation_20260814.txt` 由 486 条增至 **535 条**（多 49 条 = 8 条无句末符短名词如 宝箱/撤退/限界突破/天界/久世七暴 + 41 条 `・` 开头列表项句）。
- ⚠️ 重要发现：`ストーリー/クエスト/ステータス/ミッション/プレゼント/お知らせ/イベント` 等**不是待译节点**——它们是正文 `[ストーリー]` 方括号链接（internal-link / 角色浮窗），由链接改写引擎处理，不进 i18n key，故无法进 todo、也不靠 glossary 翻（链接显示名走 `_PAGE_SLUG_ZH` 页面中文名映射）。真正可翻译的名词碎片（宝箱/撤退/限界突破 等）已进清单。
- **固化顺序**：用户译完 → `i18n fill` 回填 official-help.json → 再由 AI 把译名写入 `glossary/terms.yaml`（译后固化，避免预填导致 extract 跳过 + 供其他页复用）。未 push。

## official-help 译文回填 + 固化 glossary（用户交稿后）
- **用户交稿**：`new_translation_20260814_translated.txt` 已译。
- **fill 两次 0 回填的根因**：
  1. `fill_todo()` 原对 official-help 用默认 `_untranslated_items(slug)`（`allow_ui_fragments=False`），与 extract 的 `True` 模式不对称 → items 不含 UI 碎片 → `[N]` 错位。已在 `i18n.py` `fill_todo` 对 `slug=="official-help"` 对称传 `True` 修复。
  2. 翻译者交付的 `_translated.txt` **丢失 `===A===` 段分隔符**（`_parse_labeled_sections` 靠它分组）→ `sections` 为空 → 0 回填。补回 `===A===` 包裹后，fill 成功回填 **534 条**（535 中 1 条中日同形被跳过）。回填后 `_todo_translate/` 清空、译文归档 `_translated_texts/`。
- **固化 glossary**：从 official-help 回填 JSON 的**节点级 keys** 提取 UI 碎片（仅 `startswith('・')` 或 无句末符且 len≤10 的短词；**排除块级整句 blocks（含 \x01 多行长句，绝不进 inline_terms 否则污染其他页）**），共 **332 条**增量追加进 `glossary/terms.yaml` 的 `inline_terms:` 段（保持原文件格式，不重排）。提供全站 render_locale 内联覆盖复用。
- **落地**：`sync-site` + `build.mjs build` 成功，`dist/zh/official-help.html` 已含译文（关于超昂大战/新手指南/招募（扭蛋）/常见问题 等）。
- **临时文件清理（用户明确纠正）**：用户重申"从未说过对删除临时文件敏感，希望不要让我看到临时文件"。本次用的 `_tmp_extract.py` 等调试脚本**执行完立即删除**，工作区无 `_tmp_*.py` 残留。已更正 MEMORY 中此前的误记（正确偏好=用完即删，不留给用户看）。
- 未 push（无用户指令）。

## b-universe「各首领攻略」BOSS 必杀技/固有效果「効果」单元格排版
- **需求**：用户点名 b-universe.html「各首领攻略」段所有 BOSS 的必杀技/固有效果「効果」单元格，参考日文页排版做 AI 语义重排（仅限此页面这些 BOSS 表格内文本，不波及他处）。
- **根因**：模板 `b-universe.template.html` 的「効果」td 已是 `data-i18n-blk` 块级标记（渲染走 `_set_block_html` 还原 `\x01`→`<br>`），但 JSON `blk.zh` 漏注入换行 → 中文全挤一行。
- **做法**：直接编辑 `data/parsed/i18n/b-universe.json` 的 21 个 blk（blk39/40/48/49/57/58/65/67/75/76/85/86/93/100/101/108/109/116/117/125/126），按日文原文 `<br>` 结构注入 `\x01` 分段，保留序号(1./2./○STANDARD・HARD)、角色分支(麦金利：/乔·泰格：等)与续行层级。
- **铁律对齐**：严格满足 `zh.count(\x01) == ja.count(\x01)`（逐块脚本校验 ALL OK），中文换行点=日文换行点；double_br 仅出现在 ja 原文本本身含空行的块(blk85/86/93/100/101/109/116/117/126)，与原文一致不视为事故。必杀技短句块(blk39/48/57/65/75/76)也按 ja br 数补换行。
- **验证**：`sync-site` + `build.mjs build` 成功；`dist/zh/b-universe.html` 确认「効果」已正确 `<br>` 分段（如 `1.出击完成时，<br>999 秒内…<br>2.体力…<br>…<br><br>〇VERY HARD<br>…`）。
- 未 push（无用户指令）。
- ⚠️ 流程教训：批量 `replace_in_file` 并行写同一大 JSON 会触发写锁超时（30s），后几个编辑失败需逐个重试；且数 ja 字段 `\x01` 不能用控制台（GBK 乱码），须用脚本 `_chk_br.py` 输出比对 br 数（用完即删）。

## b-universe 角色名拼接评论 + 浮窗失效 复发根因 & 彻底修复
- **用户复报**：清空 key 重填 i18n 后问题复发——`节拍婚礼·千麻`/`超昂阿梅兹·幻梦` 等角色名后拼 `--[ID]时间` 评论签名，且角色名浮窗失效。
- **根因（引擎 bug，非数据）**：①无链接块级块走 `_set_block_html` 整块替换时**漏调 `_strip_comment_sig`** → 任何带签名的无链接块级 blk 都会复发；②块级回退的"含角色名标记则 continue 退回节点级"xpath **排除 plugin-tooltip**（它无 `data-char`），使含 plugin-tooltip 的块走整块纯文本替换 → 清空 plugin-tooltip 结构 → 浮窗丢失。这解释为何"只清 JSON 数据仍复发"。
- **修复（i18n.py 三处）**：
  1. 无链接块级整块替换前（`_set_block_html` 处）加 `blk_zh = _strip_comment_sig(blk_zh)` 兜底剥离签名；
  2. `_strip_comment_sig` 同时应用 `_COMMENT_SIG_CLEAN_RE`（覆盖中文破折号后 `--[` 漏剥，如 `——--[`）；
  3. 块级回退"含角色名标记 continue"xpath 扩展匹配 `plugin-tooltip`（无 img 的），使其退回节点级渲染、保留浮窗结构。
- **清数据**：临时脚本遍历 `data/parsed/i18n/b-universe.json` 顶层 keyN/blkN 剥离 `--[ID]时间` 签名（顶层非嵌套 `keys`，先前误用 `data.get("keys")` 无效），搜 `--[` 全 0。
- **验证（第二次 build 后）**：全站 `--[ID]时间` 模式 0 匹配；b-universe.html 角色名包在 `<span class="plugin-tooltip">…<span style="color:blue;">中文名</span></span>` 结构完整，前端 `upgradeTooltipCharNames` 升级浮窗。✅ 未 push（无用户指令）。

## 「公式ヘルプ」侧边栏仍是日文 排查
- **现象**：用户看 dev (localhost:5173/escah) 的 zh 侧边栏，`official-help` 这条显示日文「公式ヘルプ」，其余项中文。
- **排查**：① `glossary/terms.yaml` `page_titles` 早有 `公式ヘルプ: 官方帮助中心`；② `generated/sidebar.zh.json` 第243行已是「官方帮助中心」；③ dist theme chunk 里 `官方帮助中心` 仅1次、`公式ヘルプ` 仅1次（两套 locale 数据分离，构建产物正确）；④ **根因**：dev server 是 18:00 用 `start-dev.bat` 启动的，VitePress `config.ts` 把 `sidebar.zh.json` 通过 `import` **编译进模块图**，运行中不重新读文件——后来改的 generated json 对旧 dev server 不生效，故 zh 侧边栏仍用编译时的旧值。
- **修复**：`taskkill` 旧 dev node 进程 → `sync-site` → 重启 `node build.mjs dev`。重启后 Edge dump-dom 实测 `公式ヘルプ: False`、`官方帮助中心: True`，侧边栏已中文。用户硬刷新即可。
- ⚠️ 铁律补充：**改 `generated/sidebar.*.json` / `config.ts` / 词表后，dev server 必须重启才生效（config 不受 HMR 影响）**；只 build 用旧 dev 编译、只 sync-site 也不够。验证侧边栏须 dump-dom 实测 dev，不能只读 generated json。
- ⚠️ 经验：`python -c "含中文"` 在 PowerShell 会被拆坏报 SyntaxError，须改用 `.py` 脚本文件传中文。
