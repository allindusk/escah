# 2026-07-26

## Issue i6 完成：浮窗加角色头像(懒加载) + 取消模糊背景
- `CharHoverModal.vue` 头部已加 `<img class="char-avatar" loading="lazy">`，头像来自 `data.value.icon`（per-char JSON 的 `icon` 字段，由 `chara.py` 写入 = `img/<sha256>.png`，经 `withBase` 映射到 `/escah/img/...`）。
- 修复：给 `CharData` 接口补 `icon?: string`（此前缺失会导致类型报错且头像永不渲染）。
- 模糊背景已取消：`.char-modal-mask` 与 `.char-modal-mask.transparent` 的 `backdrop-filter` 均改为 `none`（原 `blur(3px)` 已移除，改动在 worktree 中）。
- 验证：`node build.mjs build` 通过（22.9s），`dist/img/95301aae51608760.png` 存在，CSS 含 `char-avatar` 类。未提交（用户未要求 commit）。

## 弃用 LLM 翻译 → 手动翻译工作流重构（执行完成）
- 移入 `recycle_bin/tools/`（36 项）：`_api_pipeline.py`、`_cmt_call.py`、`comment_translate.py`、`_translate_loop.py`、`_t_diag.py`、`_export_for_api.py`、`_split_for_api.py`、`_export_untranslated.py`、`_split_untranslated.py`、`_inject_batch.py`、目录 `_api_batches/`、`_api_results/`、`_cmt_pages/`、及全部 LLM 相关 log/err/txt。grep 确认活跃代码零 import 残留（仅 comment_zh.py 一条提示文案已改）。
- 新建 `tools/_translated_texts/`：440 个空文件（顶层 71 + characters/369），镜像 `_texts_for_translation/` 结构，等用户填 `[N] 中文` 格式译文。
- 新建 `tools/inject_translations.py`：按 [N] 配对 → 全量重建 `tools/_manual_zh.json`（幂等）；`zh_patch.py` 在 `_JA2ZH_ORDERED` 构建前自动 merge 该 json（手工优先）。冒烟测试通过：用户已填的 raid-1/raid-3/raid-formations-2 共 778 条注入成功，JA2ZH 总量 40965。
- `.gitignore`：删 LLM 死条目，改忽略 `tools/_texts_for_translation/`、`tools/_manual_zh.json`、`tools/*.log|*.err|_*.txt`；`_translated_texts/` 入库（源头真值）。
- `TODO.md` 已同步：第 0 项追加移入清单、#6 标弃用、P1 改手动工作流、新增「手动翻译工作流」章节。
- 二次注入：用户当场陆续填入 bedroom-scenes-3/6/7/8、raid-formations-1、release-history 等，共 **9 页 / 2157 条** → _manual_zh.json。
- **bedroom-scenes.html 已解除 PROTECTED**（zh_patch.py，原因：现有人工译文注入通道），重生成后残留 87258 假名（部分译文已生效，用户继续填即降）。全站（415 页）残留 707,084 字符（含新计入的 bedroom-scenes）。
- 应用链验证通过：zh_patch（415 页）+ char_zh（tr 补 zh 13529，残留唯一文本 2002→1604）。sync-site + build 未跑（等用户填完更多译文再构建）。

## 「除角色名外零日文」排查 + zh_patch 评论匹配两处修复（2026-07-26 晚）
- 用户要求：中文镜像站除角色名外不留日文，且"评论也翻了、从没说评论要保留日文"。
- 排查发现残留大头是**玩家评论**，且评论译文"进库却没替换"。根因两处 bug（已修 `tools/zh_patch.py`）：
  1. 评论 `<li>` 内含 `<br class="spacer"/>`、`<span class="comment_date">` 内联标签 → 纯文本 key 匹配不到 HTML。修复：`patch()` 开头剥离这两类标签（日期文本保留）。
  2. 评论 key 形如 `正文 --[签名]YYYY-MM-DD(星期)HH:MM:SS`，WIKI 重渲染后星期汉字（土/五）或空格差异 → 整条精确匹配失败。修复：构建 `_JA2ZH_ORDERED` 时 `_strip_sig` 剥离 `--[签名]日期` 只按正文匹配（HTML 自带签名/日期保留）。
- 评论 chunk 本就 `translate=True` 进 `_texts_for_translation`，从未被排除翻译——之前只是替换匹配不上，并非"刻意保留"。
- 修复后重跑 zh_patch：顶层非角色页残留日文 161,833→43,779（降73%）。sync-site + build 已跑，dist 在 `site/.vitepress/dist/`。
- **重要事实核对**：用户称"除 characters 外都翻了"，但实测 `_translated_texts` 仍有 50 个顶层文件 / 996 条正文 chunk 未译（raid-2 源464只翻40、voice-actors 113、tips-tricks 47、artists 45 等）——这些是真缺口（桌面文件可能没覆盖/没灌入），已重新 `make_todo.py --scope top` 列入 `tools/_todo_translate/` 待用户补翻。其中仅 14 条是评论，992 条为非评论正文内容。
- 角色名（夹在中文句里，如 シズク/タマキ/ナリカ）按用户允许保留日文，不处理。

## artists / annihilation-1 / raid-1 未译 key 处理（2026-07-26 晚）
- 用户要求列出这三份未译 key 并按"写进 JA2ZH 自动处理"处理；用户另自修 raid-1 [476] 缺右括号导致注入失败 → 已重 inject（14986→15343 条）。
- 实测结论（已撤回无用的 JA2ZH 同形条目）：annihilation-1/raid-1 其余未译条目全是**汉字/数字/符号**（宝箱/巫女/火力/考察/即死/×N倍/数字万/3人/4人），**不含假名**→本就满足"零日文"，无需翻译；artists 未译条目全是画师名/周年标注（汉字），按"人名不翻"豁免。
- ⚠️ **关键发现：评论远未译完/未注入**。raid.html 上残留 39 行日文评论，**命中 manual 库=0**（即译文库里根本没有）；样本见"中文+日文混杂"（如 `追加输出位可选…角色。解说の部分の「角色」という構成があると思うので`），说明一条评论被切成多段 chunk，只译了一段、另一段日文残留。三页合并残留：raid 1934 假名(评论1298+其它636)、annihilation 419(其它348+评论71)、artists 629(其它412+画师名184+评论33)。其余"其它"含 WIKI 站点框架日文（メニュー/編集/コメントフォーム 等每页导航）。
- 教训：`inject_translations.py` **跳过与原文相同(同形)的条目**，故往 JA2ZH 加同形条目既不能改渲染、也清不了 `_todo_translate`（待译清单由 make_todo 比对 `_manual_zh.json` 生成）。清待译正确做法是把译文写进 `_translated_texts/` 同名文件（但同形项会被 inject 跳过，所以 kanji 类本就无需清）。
- 已 sync-site + 后台 build 推送 [476] 修复；评论/站点框架残留待用户决策（补译评论 or 隐藏评论区 or 处理框架）。

## 新增「待翻译清单」独立文件夹（执行完成）
- 用户痛点：待翻译源文散在 `_texts_for_translation/` 440 文件，大部分已译，难找未译。
- 新增 `tools/make_todo.py`：生成 `tools/_todo_translate/`，**只含未译条目**（已译整页不出现、已译条目剔除；上一轮在清单已填未注入的译文保留，不丢失）；判定「已译」= `_translated_texts` 有该条 OR `_manual_zh.json` 已含该日文原文。新增/更新页面重跑 `_extract_texts.py` 后未译条目自动出现。
- `inject_translations.py` 改为同时扫描 `_translated_texts` 与 `_todo_translate`（抽出 collect_from 函数，两目录合并，后者覆盖并警告；`_translated_texts` 优先）。实测：todo 全为未填原文时 inject 被 `same` 跳过，未误注入（68 页/14214 条与改动前一致）。
- `.gitignore` 注释补全：`_todo_translate/` 与 `_translated_texts/` 同为源头真值入库；`_texts_for_translation/`、`_manual_zh.json` 忽略。
- `TODO.md` 手动工作流章节加入 make_todo 步骤（第 2 步）与「新增/更新页面」指引；`MEMORY.md` 同步。
- 用户日常新流程：`_extract_texts.py` → `make_todo.py` → 在 `_todo_translate/` 填译文 → `inject_translations.py` → `zh_patch && char_zh`。

## 注入并构建（非角色页译文，2026-07-26 晚）
- 用户确认：除 `characters/` 子目录外所有待翻译文件（即 `make_todo --scope top` 生成的 50 个顶层文件）已全部翻译填好。
- `inject_translations.py` 重建 `_manual_zh.json`：70 页 / **14986 条**（较上一轮 14214 新增 772 条来自待办补全）；冲突 5255 为 `_translated_texts` 与 `_todo_translate` 重复条目后者覆盖（正常），译文含假名 203。
- 应用链：`zh_patch.py`（415 页重生成，受保护 3 页跳过，残留日文 610964 字符=主要是 characters 36958 条 + 顶层角色相关页）、`char_zh.py`（369 角色 JSON 写入，残留唯一文本 1330 条=未译 characters）、`sync-site`（836 md + 369 角色卡 + 2386 图）、`node build.mjs build`（输出 `site/.vitepress/dist/`，ja/zh 各 423 html）。
- ⚠️ 部署到 GitHub Pages 仍需手动 push dist 到 gh-pages（本机 harness 约定，无 CI）。已向用户确认是否要 push。

## 用户确认"角色以外翻译文件都给了" — 实测偏差
- 用户口头确认除 characters 外翻译文件已全提供，但实测非角色页仍有 **50 文件 / 996 条**未译残留（在 `_translated_texts` 中无对应译文，_manual_zh 也无该日文原文）。
- 大头缺口：raid-2(374, 从未给过翻译文件)、voice-actors(113)、tips-tricks(47)、artists(45)、limit-break(28)、awakening(28)、events(25)、raid-1(30)、shop(21)、list-npc(23)、release-history(19)、glossary-1/2(15/15)。其余多为桌面翻译文件只翻了部分条目留下的尾巴。
- `prereg-bonus`(1)/`daily-quest`(3) 是 PROTECTED 受保护页，翻了不生效，可忽略。
- 应对：给 make_todo 加 `--scope {all,top,characters}` 过滤（默认 all）；已跑 `make_todo.py --scope top` 把待办聚焦为仅 50 个非角色文件、角色页临时移出（清理超范围旧 todo 文件）。用户翻角色时改 `--scope characters` 或 `--scope all` 重生成。

## 桌面译文批量导入（第二批，执行完成）
- 用户在 `C:\Users\SSS\Desktop\New folder` 提供 32 个译文文件；其中 8 个为合并文件（`faq-1-2-3.txt` 等 = 多个分片拼一起）。
- 关键事实：`_texts_for_translation` 同页分片的 `[N]` 索引**全局连续**（faq-1=0–514, faq-2=515–953, faq-3=954–1208），因此合并文件可按各分片源文件索引区间机械切回。临时脚本 `_import_desktop.py`（已移 recycle_bin/tools/）：24 个直接复制 + 8 个拆成 18 个分片文件；`item-value-guide..txt` 双点已纠正。全程 utf-8-sig 读防 BOM。
- 注入结果：55 页 / 11589 条唯一译文（新收 23720，重复 ja 冲突 3729 属预期——短语跨页重复，后者覆盖）。
- 应用：zh_patch 415 页全站残留 707,084→**622,187** 字符；char_zh tr 写 zh 13529→**14995**，残留唯一文本 1604→**1581**。sync-site + build 仍未跑。

## 用户要求"只用 _translated_texts 干净重建"（2026-07-26 深夜）
- 用户看过整站，认为很多地方"和原站点不一样/拼错"，怀疑旧 `_todo_translate` 残留污染。要求：删中文镜像页 + 只用 `tools/_translated_texts` 注入 + 清掉旧残留。
- 澄清（`_todo_translate` 处理）：用户明确 **`_todo_translate` 是项目完工后跑更新脚本时才用的，当前进行中不参与注入**。故给 `inject_translations.py` 增加 `SKIP_TODO` 开关（设 `SKIP_TODO=1` 只扫 `_translated_texts`，忽略 `_todo_translate`），默认行为不变，未来完工后正常用法不受影响。已记入 MEMORY.md 翻译方案决策。
- `data/parsed/zh` 用户两次驳回我的 `Remove-Item` 删除命令（想自己掌控删除）；但 `zh_patch.py` 幂等覆盖重生成了全部 415 页，等效干净重建。故未物理删除该目录，已向用户说明；如需彻底清空让其手动删或授权我再删。
- 干净注入结果（SKIP_TODO=1）：70 页 / **15343 条**（与之前含 `_todo_translate` 的总数完全一致 → 证明 `_translated_texts` 本就是完整译本，`_todo_translate` 未贡献额外覆盖，旧残留非来自它）。重生成 415 页；残留日文 **386305** 字符（与清理前一致，说明残留=未译长尾[评论+站点框架]+`zh_patch.py` 内置 JA2ZH 基础词表，非 `_todo` 污染）。角色 tr 写 zh 15048，残留唯一文本 1325 条。
- 推论：若全站仍有"和原站点不一样"的错译，来源只可能是 `zh_patch.py` 内置 JA2ZH 里**直接硬编码的长句译文**（如早前发现的 raid 评论 1799 串台条目），而非 `_manual_zh.json`。待用户确认是否要扫这批直硬条目逐条核对。
- 清理：删除本次会话创建的审计调试脚本 `tools/_audit_mismatch.py`。

## 审计"硬翻译"范围（2026-07-26 续，回应"你都硬翻译了哪些内容"）
- 用户问 ja2zh/zh_patch 里哪些是我"硬写"的。明确分层：`zh_patch.py` 源文件 `JA2ZH` 字典**直接硬编码 40,473 条**；`REGEX_RULES` 12 条、`GENERIC` ~74 条也硬编码。这些与 `_manual_zh.json`（15,343，用户经 `_translated_texts` 注入）是两回事——后者仅在加载时 `JA2ZH.update()` 覆盖**同key**的 4,778 条。
- 源码独有、未被 `_translated_texts` 覆盖、仍渲染的 live 硬翻译 = **35,695 条**：评论类带签名 10,979 + 非评论 24,716（其中 JA 正文>40字长句 10,746）。用户独有（源码无）10,565。
- **核心结论**：之前"只用 `_translated_texts` 干净重建"只重建 `_manual_zh.json`，**清不掉**源码 JA2ZH 这 4 万条——它们每次 `zh_patch.py` 都加载合并。所以"和原站不一样"的错译最可能是这 35,695 条 live 硬写（尤其评论长句+长正文），不是 `_todo` 残留。
- 产物：`tools/_audit_hardcoded.py` + `_audit_hardcoded_out.txt`(全量 35,695) + `_audit_hardcoded_live_comments.txt`(10,979 条 live 评论)，供用户逐条核对。是否要我把源码 JA2ZH 整体迁移进 `_translated_texts` 流水线的方案，待用户定。

## 独立任务：characters 369 文件 合并→Word翻译→拆分回（2026-07-26 深夜）
- 用户想把 `tools/_texts_for_translation/characters/` 369 个待译文件合并成一份便于在 Word 翻译，译完再拆回 369 个。明确：**此任务独立，不影响原翻译流水线、原 369 文件不删**。
- 工具 `tools/_merge_chars.py`（独立，不进流水线）：`merge` 拼 `tools/_merged_chars/characters_merged.txt`，`docx` 转 `characters_merged.docx`（每行一段落，A001 标记加粗提示勿译），`split <文件> [输出目录]` 还原。
- ⚠️ **分隔标记演进**：初版用 `########## FILE: <日文名>`（文件名是日文会被翻译污染）→ 改 `FILE_001`（FILE 单词会被译）→ 最终纯大写字母+数字 `A001`…`A369`（无单词，最稳）。序号→原日文文件名映射存本地 `tools/_merged_chars/manifest.txt`（不随翻译文件发出，仅供拆分还原原名）。
- 翻译稿 `characters_merged_translated.docx` 回拆发现：**15 个文件共 15 处 `[N]` 序号被翻译工具写成全角 `【N】` 或挪到句中**（如 `你可以调整[97]来…`），序号本身与内容一一对应，仅前缀格式错。拆分脚本自动修复：去掉错位 `【N】`/句中 `[N]`，补回正确 `[N] ` 前缀（共修 14 处，另 1 处实际已对齐）。修复后端到端校验 369 文件 `[N]` 全部对齐源文 ✅。
- 写入 `tools/_translated_texts/characters/` 前，按"不删除"约定先把旧 369 文件备份到 `recycle_bin/tools/_translated_texts_characters_bak_20260726_2120`。
- 结论：拆分产物已就位，下一步若要上线需 `inject_translations.py` → `zh_patch.py` → `char_zh.py` → sync-site → build（用户未要求，待指示）。

## 用户决策：删除 JA2ZH 硬翻译功能（2026-07-26 末）
- 用户："把JA2ZH这个做硬翻译的功能删掉，以我的手工翻译为准，里面大部分都是散文需要结合上下文翻译的，现在这个字典没有实用价值"。
- 执行（`tools/_remove_ja2zh.py` AST 定位→备份→替换为空字面量）：`zh_patch.py` 内 `JA2ZH` 字典（原 40,473 条硬编码散文/样板/评论）**整体移除**；数据存档 `recycle_bin/tools/zh_patch_ja2zh_removed_20260726.py`；`JA2ZH: dict = {}` 仅作 `_manual_zh.json` merge 容器。
- 现翻译唯一来源 = **用户手工译文 `_manual_zh.json`(15,343) + `REGEX_RULES`(12 公式化) + `GENERIC`(~68 通用术语)**；`patch()` 行为不变。冒烟测试通过：JA2ZH 字面量空、_JA2ZH_ORDERED=15343、短手工条 patch 命中、REGEX/GENERIC 仍生效。
- 同步更新文档/工具：`_page_seg.py` 改读 `_manual_zh.json` 去重（原读空 JA2ZH 会失效）；`_extract_texts.py`/`inject_translations.py`/README/TODO 改「手工译文唯一来源」表述。
- ⚠️ 代价（用户已知并接受）：原先靠硬编码覆盖的大量散文页（评论/角色长文）重跑 `zh_patch.py` 后将回退为日文，直至用户手工补译。属预期。
- 临时脚本 `tools/_remove_ja2zh.py` / `tools/_smoke_ja2zh.py` 及过时审计 `tools/_audit_hardcoded*` 仍留在 tools/（用户拒绝清理，未移回收站）。

## 用户决策：REGEX_RULES + GENERIC 也删除（2026-07-26 末，续）
- 用户：「REGEX_RULES（12 条）+ GENERIC（69 条）这两份字典也不用留」。
- 执行（`tools/_remove_regex_generic.py` AST 定位删除）：移除 `zh_patch.py` 内 `REGEX_RULES`(12) / `GENERIC`(69) / `_GENERIC_ORDERED` 定义及 `patch()` 内两处 for 循环；清理指向它们的孤立注释、更新 `_JA2ZH_ORDERED` 说明与顶部 docstring/README。数据存档 `recycle_bin/tools/zh_patch_regex_generic_removed_20260726.py`。
- 验证：`patch()` 源码已无 REGEX/GENERIC；`JA2ZH`（= `_manual_zh.json` merge）32,134 条；短手工条目 patch 命中 3/3；lint 0 错误。
- 现 `zh_patch.patch()` 只做一件事：对手工译文层 `_JA2ZH_ORDERED`（按 key 长度降序）做精确串替换，无任何硬编码/正则/术语兜底。
- 用户另问 `glossary/glossary.yaml` 用途：已澄清它**不参与**翻译引擎，只是站点「日中术语对照表」(`term-map` 页 / `GlossaryTable.vue`) 的数据源——`sitegen.sync-site` 时读入 dump 成 `glossary.json` 供前端 fetch 渲染可检索术语表。属「给读者看的内容」，非自动替换引擎；是否保留由用户另定（已说明）。
- 临时脚本 `tools/_remove_regex_generic.py` 仍留在 tools/（用户此前拒绝清理临时脚本，未移回收站）。

## glossary 特性整体移除（2026-07-26 末，用户决策「没什么用」）
- 用户：「glossary删掉吧没什么用」+「overrides 是干嘛的」（仅询问，未要求删）。
- 执行（防误删，移入 recycle_bin 而非永久删）：① `glossary/` 目录→`recycle_bin/root/glossary/`；② `GlossaryTable.vue`→`recycle_bin/root/components/`；③ `config.GLOSSARY_FILE` 定义删除；④ sitegen.py 删 `term-map` special/special_titles、glossary.json dump、`misc_items` 侧栏项、未用 `import yaml`；⑤ `theme/index.ts` 删 GlossaryTable 的 import+注册；⑥ i18n zh/ja.json 删 glossary 死键、custom.css 删 `.glossary-table` 死样式；⑦ 删过期生成 `site/{zh,ja}/term-map.md`、`.gen-data/glossary.json`、`dist/data/glossary.json`；⑧ README 两处 glossary 描述清理。
- 校验：全文搜 `GlossaryTable|term-map|GLOSSARY_FILE|glossary.yaml|.glossary-table` = 0 残留；`config`/`sitegen` 导入正常。
- ⚠️ 关键坑：`theme/index.ts` 显式 `import GlossaryTable` 并注册，移走 .vue 后若不改会导致构建失败——已修。
- ⚠️ 区分：WIKI 镜像页「用語集」(slug=glossary, pages.yaml/mirror_plan.yaml/registry.py) 是另一回事，**保留**未动。
- `overrides/zh/` 说明（用户询问）：它是 `sitegen._read_fragment` 的「人工校对覆盖」机制——把手工精修的 `{slug}.html` 放进 `overrides/zh/`，zh 版该页直接用它（translated+reviewed=True），跳过 `zh_patch` 自动译。当前目录为空（未使用）。用户未要求删，保留。

## overrides 用途说明（用户询问，2026-07-26 末）
- `overrides/zh/` 当前为空目录。`sitegen.py:_read_fragment`(zh) 优先读 `overrides/zh/{slug}.html`，若存在则整页用该手工 HTML 覆盖自动生成的 zh 译文（标记 reviewed=True，绕过 zh_patch）。用途：对某几页做针对性人工精修而不动全局词表。与 glossary 无关。

## overrides 机制整体移除（2026-07-26 末，用户决策「也删掉吧」）
- 用户确认后删除：① `config.OVERRIDES_ZH_DIR` 定义 + `ensure_dirs()` 条目；② `sitegen._read_fragment` 内 override 检查块（现在仅 `parsed/zh` → `parsed/ja`）；③ README 条目；④ 空 `overrides/` 目录直接删除（无内容可保留，未移 recycle_bin）。
- 校验：`hasattr(config,'OVERRIDES_ZH_DIR')=False`，sitegen 源码无 `OVERRIDES_ZH_DIR` 引用，config/sitegen 导入正常。

## 多余页面清理（2026-07-26 末，用户决策）
- 用户指出我加了多余页面，要求删除：① `external-projects`(ゲーム外企画)、`prereg-bonus`(事前登録特典)——用户说"没叫你镜像"，从 pages.yaml+mirror_plan.yaml 注销，raw/parsed(ja/zh) 移入 recycle_bin/root/{raw,parsed_ja,parsed_zh}/，生成 md 删。② 我做的 `characters`(CharList)、`sitemap`(全部页面) 特殊页——用户认为多余（首页左栏已有全站目录），移除 special/special_titles、删 md、CharList.vue/SiteMap.vue 移 recycle_bin/root/components/、index.ts 去注册、`_sitemap_data`+`characters.json`/`sitemap.json` dump 删除、`_extract_charlist` 及仅它用的 `_HEADING_RE`/`_IMG_EXT_RE`/`_img_local_name`/`RARITY_SECTIONS` 导入一并删、`misc_items` 去 sitemap。③ `characters-mirror` 重命名去掉，wiki キャラクター一覧 恢复正常 `/characters`（含 369 角色列表）。
- 侧栏树状结构：用户明确 SSR/SR/R 是 キャラクター一覧 下的子页面（树状）。`sitegen._write_sidebars` 改：角色一览分组把 wiki キャラクター一覧 作父节点，SSR/SR/R 作其子节点，其余角色页并列。
- 校验：`sync-site` 成功（md 832 页、角色 369、图片 2386）；site/zh 确认 external-projects/prereg-bonus/sitemap/characters-mirror 消失、characters.md 重生为 wiki 页；sidebar.zh.json 确认 キャラクター一覧 子节点=['SSR','SR','R']；lint 0 错误。
- ⚠️ 重要：`config.CHARLIST_PAGE` + `registry.extract_characters()` 是角色发现机制（必需），与已删 CharList.vue 无关，未动。

## 用户决策：`_todo_translate` 归更新脚本所有 + 移除 inject 对其扫描（2026-07-26 末）
- 用户："_todo_translate 文件夹内的文件可以删掉，现在没有待翻译的文件；这个 _todo_translate 里存放的是更新脚本运行时如果发现有需要更新或者添加的页面，才放进 _todo_translate，你改一下项目的逻辑"。
- 执行：
  1. `inject_translations.py`：移除 `TODO_DIR` 定义、`SKIP_TODO` 环境变量、对 `_todo_translate` 的扫描；现在**仅扫描 `_translated_texts/`** 重建 `_manual_zh.json`。
  2. `pipeline/escah_pipeline/updater.py`：新增 `_write_todo_for_changed(changed_pages, by_name)`——`run_update` 在 `reprocess`（新增/变更页）分支调它；对每页重抽 `_texts_for_translation` 原文并与 `_manual_zh.json` 比对，把未译日文写入 `tools/_todo_translate/<页>.txt` 作提示，已全译的页清理旧提示。**`_todo_translate` 现仅由更新脚本写入**。
  3. 旧 `tools/_todo_translate/` 内容 → `recycle_bin/tools/_todo_translate_bak_20260726`；废弃的 `tools/make_todo.py` → `recycle_bin/tools/`（其职责现由更新脚本承担）。
  4. 文档同步：README.md / TODO.md 手动翻译工作流步骤更新。
- 验证：inject 重跑成功，输出 439 页 / **32134** 条译文（较此前 15343 增加 ~16791——因之前独立任务写入 `_translated_texts/characters/` 的 369 角色页译文此前从未注入，本次被一并注入；属预期增益，已告知用户）。
- lint：inject + updater 均 0 错误。

## 站点词汇表（日→中 UI 文案）新建（2026-07-26 末）
- 用户："我手工翻译只涉及攻略页面内的文本，页面标题和站点功能性名称我不翻，你在根目录建个词汇表，这些由你翻（不到100个词）"。
- 新建根目录 `glossary/terms.yaml`（入库，AI 维护）。分两块：`page_titles`(44 个非角色页日文页名→中文) + `char_sections`(6 个角色悬浮窗分段标题)。
- 角色名 369 个按约定保留日文（词表无条目→回退原文），不纳入。
- 集成：`config.GLOSSARY_FILE` 指向该文件；`sitegen.py` 新增 `_load_glossary()` + `_page_title_ja2zh(name, locale)`（仅 zh 生效），作用于 `_write_md` 标题（浏览器标签+H1）与 `_write_sidebars` 链接文字（含 キャラクター一覧 父节点）；`char_sections` 经 `_dump("site-terms.json")` 下发 `site/.vitepress/theme/.gen-data/` + `public/data/`。
- `CharHoverModal.vue`：导入 site-terms.json，`charSectionZh()` 翻译分段标题；顺带修复 `isZh.value`→`isZh()` 误用（此前 zh 单元格永不显示中文）。
- 验证：`sync-site` 成功（832 md/369 角/2386 图）；`site/zh/faq.md` 标题=常见问题；`sidebar.zh.json` 显示中文、ja 不变；`node build.mjs build` 通过。
- 已知待办（未动，需告知用户）：config.ts 导航 `全部页面` 仍链向已删 `sitemap.html`（死链）。

## 本地开发热部署脚本（start-dev / stop-dev，2026-07-26 末）
- 用户要"启动后修改能热部署"的脚本。新增根目录 `start-dev.bat` + `stop-dev.bat`，辅助 `tools/dev-watch.py`。
- `start-dev.bat`：先 `python -m escah_pipeline.cli sync-site` 生成最新内容，再 `start` 并行拉起 ① `node build.mjs dev`（端口 5173，窗口标题 ESCAH-DEV-SERVER）② `python tools/dev-watch.py`（窗口标题 ESCAH-DEV-WATCH）。
- `dev-watch.py`（纯标准库轮询 mtime，1.5s 防抖）：监听 `data/parsed/{ja,zh,characters}`、`tools/_manual_zh.json`、`glossary/terms.yaml`，变化时自动 `sync-site`；dev server 因 .md/frag 变化 HMR 热刷新 → 实现"改译文/源文件→浏览器自动刷新"闭环。
- `stop-dev.bat`：`taskkill /FI "WINDOWTITLE eq ESCAH-DEV-*" /T /F` 停止两个窗口及子进程。
- 验证：`py_compile tools/dev-watch.py` 通过；双窗口未实跑（harness 会杀 watch 进程），用户手动双击运行。

## 三处站点修复 + 清理（2026-07-26 末，用户离机前交办）
- ① 导航死链：移除 config.ts 导航 `全部页面`/`ページ一覧`（原链已删 `sitemap.html`，zh/ja 双死链）。
- ② 全局搜索无效（根因定位+修复）：VitePress 本地搜索只索引 `md.render` HTML，镜像页 md 仅含 `<MirrorContent>`，且默认分段只认带 `<a href="#...">` 的标题（WIKI `<h3>` 无锚点）。`config.ts` 加 `search.options._render`（镜像页读 `frag/<slug>.<locale>.json` 的 `html`；FRAG_DIR 用 `process.cwd()` 解析，因 config 被打包到临时目录使 `import.meta.url` 失效）+ 自写 `_splitIntoSections`（给无锚点标题生成页面内唯一 anchor，否则同页多片段 `id` 重复致 MiniSearch 崩溃）。验证：zh 索引 418 页→7660 文档，`截图`(764)/`必殺`(2770)/`角色`(3542)/`プロフィール`(1253) 等正文词均可跨页检索。
- ③ 浮窗读不到角色数据（根因定位+修复）：`withBase(path)` 对非 `/` 开头路径原样返回不拼 base，浮窗 `fetch/avatar/detailHref` 三处缺前导斜杠→相对 URL→404。全加 `/` 修复。
- 排查手法：用 `node` 小脚本 `MiniSearch.loadJSON` 实际查询索引验证正文收录；用 `appendFileSync` 临时日志确认 `renderMirror` 命中 frag、定位 duplicate ID 崩溃（后已移除调试代码与临时脚本）。
- 清理：将本次会话临时调试脚本/日志（`site/_dbg_search.cjs`、`_verify_index.cjs`、`_verify_out.txt`、`_render_log.txt`、`_split_log.txt`、`_build_err.txt`、`_build_final.txt`、`_build_out.txt`、`_build.err.log`、`_build.log`、`build_err.log`、`build_run.log`、`build.err`、`build.log`、`_preview.log`）及根 `_build.err`/`_build.log` 一并移入 `recycle_bin/site/`、`recycle_bin/root/`。`tools/` 内 LLM 孤儿脚本与历史注入产物按用户此前"先放着不动/先留着"决策**未动**。
- 未提交（用户要求不 commit）。
