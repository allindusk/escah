# 2026-07-24

## 镜像计划配置 + 本地自动更新（按用户需求）
- 解耦出 `data/registry/mirror_plan.yaml`（planned / mirrored 11 组分组有序），由新增 `pipeline/escah_pipeline/plan.py` 与 `sync-plan` 子命令维护。
- 改造 `updater.run_update`：planned 处理 + RSS 增量/`--full` 逐页 last-edit-time 检测 + 子进程跑 `tools/zh_patch.py`（唯一翻译路径，弃用 LLM）。
- `config.py` 增 `MIRROR_PLAN_FILE`；`snapshot.py` 的 `record_page` 增 `wiki_last_modified`；`fetcher.py` 增 `parse_wiki_lastmod`。
- **已取消 GitHub Action 工作流**：用户决定自动更新走本地脚本（CI 内 AI 无法补译新词条）；`translate` 命令已改为跑 `zh_patch.py`。
- 本地 `sync-plan` 已运行，生成 418 页分 11 组、planned 空的 mirror_plan.yaml；模块 import 自检通过。
- 下一步：用户想更新时来对话里让我跑 `update`（网络抓取按需触发，未在此次执行）。

## 恢复翻译（zh_patch 词典补译）
- 新增 `tools/_addraid.py`（复用 `_addfaq2.py` 注入模式），向 `tools/zh_patch.py` 注入 30 条 raid 页术语/散文（レイドボス/RAID推荐增益手・减益手/BREAK相关说明等，added=30, skipped=0）。
- 已跑 `python tools/zh_patch.py` 重建 414 页中文（跳过 4 受保护页），全站残留日文合计约 91.5 万字符（主要在角色页散文，后续继续按页补译）。
- 续译策略：`tools/_page_seg.py <页名>` 生成待译句候选（按频率排序），写 `_add<页>.py` 注入后重跑 zh_patch。

## 批量续译批次A–E + 角色JSON补译工具 + glossary slug 冲突修复
- 新增 `tools/char_zh.py`：复用 zh_patch 的 patch() 给 `data/parsed/characters/*.json` 的 tr 单元格补写 `zh` 字段（幂等，词表增长后重跑）；残留报告写 `tools/_char_remain.txt`。当前 16491 个 tr 单元格中 ~11000 已命中写 zh。
- 新增 `tools/_freq_chars.py`：全局扫 369 角色页 ja 源，按 (频次×长度) 排序输出未译净文本 → `tools/_seg_chars.txt`。
- 注入批次A–E（`tools/_addchars_a.py`~`_addchars_e.py`）：A=角色页评论规则样板31句(368页复用，收益-31万) + 高频散文65条；B=freq≥2机制句36条；C=技能公式短语42条+正则6条；D=角色小知识长散文36条；E=glossary页词条46条。
- **全站残留假名：约101万 → 68.8万**（顶层49页39万→~36.6万；角色页61万→~32万）。
- **修复 slug 冲突 bug**：`sitegen.py` 特设页「日中用語対照表」原用 slug `glossary`，会覆盖 WIKI 镜像页「用語集」(slug=glossary) 的 md 导致用語集在站点不可见。特设页已改名 `term-map`（special/special_titles/nav 三处），glossary 现为用語集镜像页。
- 每批均 sync-site + `node build.mjs build`(detached) 验证：build 22s 成功，dist 中评论规则译文740页、glossary 镜像页译文、term-map、369 个角色 JSON 含 zh 字段均确认。
- 剩余待译：顶层 faq(2.4万)/raid(2.1万)/event-list 等 + 角色页每页独有散文(freq=1 长尾约 1.8 万条唯一文本)；JSON 残留唯一文本 ~2900 条（多为卡面介绍散文，与角色页散文同源，补词表双向受益）。

## 续译 F–M 收尾（本会话延续）
- **G 批次 raid 系列全部完成**：raid / raid-recommended(18 角色名终态) / raid-buff-debuff(1 名终态) / raid-formations(4 名终态)。过程中修复两处根因 bug：
  1. `zh_patch.patch()` 入口对文本与词表 key 统一 `unicodedata.normalize("NFC", ...)`，消除源文件 NFD 长音「ー」分解导致的精确键失配。
  2. 多处值误用简化字 `昂る种`（种 U+79CD）与 GENERIC 键 `昂る種`（種 U+7A2E）码点不同，使 GENERIC 未命中、残留平假名「る」；已改为直接写中文化 `绽放的种子`（或 traditional 種 让 GENERIC 命中）。
- **L 批次系统页全部完成**：wide-battle(483 段全译，剩 2 角色名终态)、battle(482 段全译，剩 1 角色名终态)、skills(机制碎片全译，剩 152 技能名/角色名终态)、unique-effects(碎片全译，剩 363 效果名/角色名终态)、list-ssr(散文1句译，剩 37 角色名终态)。角色名/技能名/固有效应名/道具名按规则保留日文（与 raid 页终态一致）。
- **M 批次角色页**：`_freq_chars.py` 重跑显示 18411 条唯一未译节点(63万字符)，Top400 绝大多数为角色名/称号(保留) + freq=1 每角色独有长篇背景散文(长尾，逐条不可穷尽)。补译 6 条高频非名碎片。
- `char_zh.py` 重跑：369 角色 JSON、303 变更、10992 tr 单元格写 zh、13112 已净/同形、JSON 残留唯一文本降至 2396 条（多为技能名保留 + 部分已译效果描述碎片）。
- **流水线收尾验证**：`python -m escah_pipeline.cli sync-site`(836 md / 369 角色卡 / 369 数据 / 2386 图) + `node build.mjs build`(detached) → build complete 36.39s，dist 正常。
- **结论**：顶层系统/攻略页（faq/raid/wide-battle/battle/skills/unique-effects/list-ssr）散文已基本译完，仅留专有名词；角色页结构化数据已大幅翻译；剩余长尾＝角色页独有背景散文(18k 唯一)＋2396 条 JSON 残留(技能名/效果描述碎片)，需后续按页迭代补译，单会话不可穷尽。

## 续译 N 批次：b-universe/limit-break/equipment/items/gacha/杂项（本会话）
- **b-universe 完成**：`_build_buniverse1.py`(idx449-650, 202条) + `_build_buniverse2.py`(idx651-826) → 379→17 终态。
  ⚠️ 修复 buniverse2 错位 bug：TR 漏掉 idx798「あの日の決意」(源中 798/799 重复出现)导致后续全部+1 错位、还误收 idx827(limit-break 段)；
  先用正则从 zh_patch.py 删除错误 buniverse2 块再重注入。教训：**源段重复出现时 idx 序列会连续占位，TR 必须逐条对齐，勿跳号**。
- **limit-break 完成**：`_build_lb1.py`(idx828-957, 130条) + `_build_lb2.py`(idx958-1125, 168条) → 298→0 全清。
- **equipment 完成**：`_build_equip1.py`(140条) + `_build_equip2.py`(129条) → 273→3 终态(装备名恒等映射)。
- **items 完成**：`_build_items1.py`(262条全量) → 262→2 终态(さやか/活动名保留假名)。
- **gacha 完成**：`_build_gacha1.py`(66条机制散文) → 140→74 终态(卡池名/限定角色变体名保留日文)。
- **unique-effects/skills 补碎片**：`_build_ue_sk.py`(85条半角片假名机制碎片) → ue 363→279、sk 152→146，剩余全为效果名/技能名终态。
- **glossary/artists/annihilation**：`_build_misc1.py`(32条散文，25+3+4)。glossary 剩 49 中散文实已译(patch 验证 OK)，残留计数是译文中保留的角色名假名；artists 剩画师名终态。
- 各页 min6 残留终态快照：faq 5 / raid 6 / raid-formations 4 / battle 1 / wide-battle 2 / annihilation 12 / raid-recommended 18(全名) / characters 105(变体名+剧情标题)。
- 流水线收尾：zh_patch → char_zh → sync-site → `node build.mjs build`(detached，输出 tools/_build_out.txt)。

## LLM API 翻译管线启动（21:40）
- 用户提供腾讯 TokenHub 三个 GLM 模型（glm-5/5.1/5.2）各 100 万 token。key: `sk-G0x4kZk3tHlmXzQelMxy5N2WJ2mTOqMnR6oOfFHzQ0stkShz`, url: `https://tokenhub.tencentmaas.com/v1`。
- 新建简化版翻译运行器 `tools/_run_translate.py`：段落级 28208 段纯日文 → 8 batch → 自拆子批 → 三模型轮换 → 自动注入。
- GLM 关键发现：模型先输出 reasoning（~800-1300 tok）再输出翻译内容，`max_tokens` 必须设高（64000）。第一次运行因 max_tokens=500 全失败。
- **当前进度**：batch 001 的 7 个子批前 3 个已通过 ✓（PID 7816 后台运行中）。后续 batch 2-8 待处理。
- `translate_chunk()` 返回 (zh_list, prompt_tok, comp_tok)，tok 追踪用 `getattr`（兼容 dict/object）。日志行缓冲 `buffering=1`。
- **注意**：残留探测 `_remain_segs` 是在 zh 文件上扫假名，译文中按规则保留的日文名会被计入——"残留数>0"不等于未译，需抽样 patch() 验证。

## 续译 O 批次：角色页全局高频散文（`_freq_chars.py` 长尾）
- 顶部系统/道具页译完后，剩余长尾＝369 角色页各自独有背景散文。用 `python tools/_freq_chars.py` 在 369 角色页上全局统计未译净节点（freq×len 排序→`_seg_chars.txt`），18383 唯一 / 633827 字符。
- 关键发现：**散文段嵌入日文名（トキサダ/ルビー等片假名）时，整段仍被残留探测器计入**；但若译文把嵌入的名写作汉字/中文（时都/露比…），整段无假名即可整段清零。故角色页散文要"清零"需连嵌入名一并汉字化（与顶层页"保留日文名=终态"不同——顶层页多为短名段，角色页为长散文）。
- **批次1** `_build_chars1.py`(57条→注入40/跳17，最长散文 idx6-106)：因多处保留片假名名未清零，多为"已译但名残留"。
- **批次2** `_build_chars2.py`(46条全注入，idx130-168+跨页高频片段136/159/179/180/182/184/206/243/244/258)：名写作汉字→整段清零。
- **批次3** `_build_chars3.py`(20条全注入，idx171-200散文)：同上。
- 累计会话内唯一残留 18383→18270（清零113段）。剩余 18270 唯一仍巨大——单会话不可穷尽，需按页/按批持续迭代。
- 已跑 zh_patch→char_zh→sync-site，并 `node build.mjs build`(detached) 发布。
- **遗留脚本**：`_build_chars1/2/3.py`、`_t.py`(测试用，可删)。`_batch_chars*.json`、`_seg_chars.txt`、`_rg_*.txt` 均为过程产物。

## 续译 P 批次：最长散文批量优先 + 跨页高频片段加杠杆（用户授权"你决策"）
- 决策：**最长散文（freq=1，100+字符）批量优先**为主攻方向（直接缩小 60 万字符主堆），每批顺带收割 freq≥2 的**非专有名词**片段（零成本、清多页）。专有名词/服装变体名（如`正月エスカレイヤー`/`閃忍ナリカ`/`聖夜エクシール`）按规则保留日文=终态，跳过不译。
- **批次4** `_build_chars4.py`(85条→注入83/跳2，idx200-280散文 + 跨页高频片段221/223/227/231/251/280/292/294/295/299/300/301/357/359)：残留 18270→18187。
- **批次5** `_build_chars5.py`(52条全注入，idx281-340散文)：残留 18187→18135。
- 本会话（O+P）累计唯一残留 **18383→18135**（清零248段），字符 **633827→605221**（清约2.86万字符）。已跑 zh_patch→char_zh→sync-site→`node build.mjs build`(detached)。
- 已固化汉字化译名表（角色页散文用）：时都/迪比特/露比/莉可丽丝/莉露卡/琪露卡/彩香/诺诺诺/诺洛依/鹰丸/天音/遥/玛琳娜/六香/阿尔法/阿尔戈尔/托帕兹/白羊座/茜/莱卡/司/浓香/普拉提涅尔/艾莉/薇卡/库萨里/乌尔扎/泽斯/梅格·尤金/乌洛波洛斯/梅加美/莉莉/鲁克塞尔/托瓦尔卡因/阿兹尔/戴拉斯特/鸠/凯/宙斯/乌拉诺斯/盖亚/克洛诺斯/提坦/米戈/霍华德/克苏鲁/奥德赛/奥德修斯/杰克·南瓜灯/开膛手杰克/曼德拉草/哈利波特/假面骑士Mach/信号斧/密斯蒂克托帕兹/密斯蒂卡尔/招财猫/喧闹老虎/电光急性子/轻栗/勇气十姐妹/冲击腰带/哔哔啵啵/死亡脉冲爪/最深沉之伤/Bind the Tuna/沉默之声/久世染/替身树Ⅱ/扩声木R/猫屋敷/姬路/斯威特/白子/斗羽/羽折/薄命/鸭/凯/鹫一/深山初音/比良坂初音/志津香/凪/高圆寺/沙由香/恭平/源太郎/阿美兹/阿卡里/菊理/哈鲁卡/爱丽丝软件/斗神都市/兰斯/对魔忍/上弦众/火者/火一族/幻魔/接现力/雷加利亚/神骑/九大神骑/天使长/龙之子/艾斯卡蕾雅/艾克希尔/娜莉卡/蓝宝石。
- 注：`_inject_batch.py` 期望 JSON 为 `[[ja,zh],...]` 列表（非字典）；`_build_chars4.py` 初版误用字典导致报错，已改为列表格式。后续批次脚本沿用列表格式。
- 剩余 18135 唯一仍巨大——长尾需持续按批迭代；下批可继续 idx341- 散文或转 `_page_seg.py <页>` 啃单页。

## 续译 Q 批次：freq≥2 跨页共享片段（用户选方案3，杠杆最高）
- 策略：从 `_seg_chars.txt` 顶部（按 freq×长度排序）筛出 **freq≥2 且能"完全译净(无残留假名)"** 的项。顶部高频多为服装/角色专有名词（如`真夏のハルカ`/`ブライドハルカ・リバース`），保留日文=终态跳过；夹杂的**纯游戏术语/机制散文**才是目标。
- **`_batch_seg1.json`**(17条全注入 added=17/skip=0)：高频清多页样例——`ネタバレ要素あり→含剧透要素`(freq14)、`横一列必殺は向いていない→不适合横排一列必杀`(freq11)、`スタミナが50％以下になった時、→体力降至50％以下时、`(freq7)、`好感度アイテム・限界突破アイテム使用1/2`(freq7各)、`※は『真夏』に関係する効果を所持`(freq6，注：真夏=纯汉字可保留)、`にて「昂る種」300個で交換可能`(freq6)，及含 `グナガン→格纳甘` 的机制散文等。
- **直接改 zh_patch.py 两条既有残缺条目**（injector 会 skip 已存在 key，故手改）：① line8103 `（素敵な）リコリス様の…`(自映射未译,freq4)→中文译文；② line10386 `ストーリー2部2章…`(既有条目却保留 イノリ/ライカ 假名,freq2)→`伊诺里/莱卡`译净。
- 新增/确认一致译法：`グナガン→格纳甘`、`リコリス→莉可丽丝`、`ジブリール→吉布列尔`、`昂るつぼみ→绽放的花蕾`、`昂る種→绽放的种子`、`限界突破`(纯汉字直用)、`ネタバレ→剧透`、`真夏`(纯汉字可保留)、`横一列→横排`、`スタミナ→体力`、`フロントウイング→Front Wing`、`イノリ→伊诺里`(既有,站内混用)。`ビートブレイカー` 无既定译法→暂跳过。
- 结果：唯一残留 **18135→18116**（清19段），字符 **605221→603069**。因全是 freq≥2，页面级实例清除量为 freq 加权数倍。已跑 zh_patch→char_zh→sync-site→`node build.mjs build`(18.95s,dist更新)。
- 教训：`_seg_chars.txt` 里 freq≥2 的行**可能已有 zh_patch 条目但译文仍含假名**（专有名词未展开），这类不会自动清除，须直接改 zh_patch.py（injector 不覆盖已存在 key）。

## 续译 R 批次：seg2 混合批（freq≥2 残余 + 顶部最长散文）
- 现状判断：`_seg_chars.txt` 顶部 freq≥2 剩余几乎全是**服装/角色卡名**（保留日文=终态），可译高频项已在 Q 批清完。故转混合收割：2 条 freq≥2 可译项 + 顶部 freq=1 最长散文（130–290字符）。
- **`_batch_seg2.json`**(30条→注入14/skip16)：skip 的 16 条为已有条目（多数已有含假名译文=规则内终态，如夜が来る同一存在句）。注入的含：ルビー与エスカレイヤー对比论、くさり(库萨里)人物背景、忍者美竹スバル/鷹守はる 档案句、雪月花梗概、レイドボス回避debuff计算(1.6倍)、恒常キャラ蝶矿石UP更新、兰斯"世界的BUG"句、谦信毗沙门天/火星考证、齐格弗里德词源等。
- **直接改 2 条自映射/半译**：`ハニ噛み王子→蜂蜜咬王子`(freq7)、`ひつじ小屋別館（移転先）→小羊屋别馆（迁移地址）`(freq14，原半译)。
- 新增译名：`齐格弗里德`(ジークフリート)、`法夫纳`(ファヴニル)、`玛迪翁`(マディオン)、`杰雷米`(ジェレミー)、`乌拉拉`(うらら)。未定名保留假名：コバリ/アサギ/スケートル/ムーンライズ/オニキス/リシュリー/ショウコ/スバル/はる/りるか/ハルヒーナ。
- 结果：唯一残留 **18116→18103**，字符 **603069→601223**。本会话累计 633827→601223（清约3.26万字符）。全链路跑通，build 19.81s dist 更新。
- 判断：freq≥2 可译库存已基本耗尽（余下高频=卡名终态），下一步主路径回到 freq=1 长散文批量或 `_page_seg.py` 逐页清扫。

## 续译 S 批次：chars6 长散文（idx200-280，字符跌破60万）
- **`_batch_chars6.json`**(51条全注入 added=51/skip=0)：idx200-280 的 freq=1 散文（91-110字符段）+ 2条 freq=7 短语（`自身の攻撃で敵を倒すたび、→每当以自身的攻击打倒敌人、`、`キャラ紹介ブログへのリンク→角色介绍博客的链接`）。
- **直接改 1 条自映射**：`1ダースべべたー→一打贝贝塔`（line7464，沿用既有 べべたー→贝贝塔 译法）。
- 查表确认既定译法（写入前先 grep zh_patch）：`龍の者→龙之者`（⚠️ 不是"龙之子"，龍の子 才是龙之子）、`ビートアイドル→节拍偶像`、`ノドカ→浓香`、`ミミック→拟态怪`、`コンボの謎→连击之谜`、`べべたー→贝贝塔`、`サイレントヴォイス→沉默之声`、`マリナ→玛琳娜`。
- 新增译名：`春奈`(ハルナ)、`美杜莎`(メデューサ)、`布拉姆·斯托克`/`德古拉`/`弗拉德三世`、`《大连者》`(ダイレンジャー)、`《有言实行三姐妹舒舒特莉安》`、`《飞女刑事》`(スケバン刑事)、`桔梗太夫`(汉字直用)。未定名保留假名：ピカ/エール/ザイン/リゲル/ザトゥーナ/アステライズ(散文内)/ムーンライズ/サンライズ。
- 结果：唯一残留 **18103→18060**，字符 **601223→596994**（**跌破60万**）。本会话累计 633827→596994（清约3.68万字符）。全链路+build 34.34s 完成。

## HY3（腾讯混元 hy3）API 加速翻译（两阶段·阶段一落盘，未注入）
- 用户申请 HY3 API 并要求接进来加速。已建 `tools/llm_patch.py`（仅标准库 urllib，零依赖）。硬编码 base_url=`https://tokenhub.tencentmaas.com/v1`、model=`hy3`；API Key 存根仓库根 `.env`（已被 gitignore），从 `HY3_API_KEY` 读取，绝不入库/提交。
- 子命令：`collect`（扫 `data/parsed/ja/*.html` 跳过 PROTECTED 页 + 角色 JSON 跳过 CV/イラスト/本名等专名行，抽 `>`标签间含假名纯文本片段`去除重→`_llm_source.json`，按 count 降序）→ 产出 **24901 条唯一待译片段**；`translate`（分批调 HY3，断点续译+重试退避，写 `_llm_translations.json`，本阶段不注入词表）；`inject` 阶段二预留。
- **强化翻译提示词**（用户明确要求）：运行时读 `glossary/glossary.yaml`(59 条固定译法) 注入 system 提示词，并加游戏术语细则（ステータス=属性、ガチャ=扭蛋、レイド=讨伐战、バフ/デバフ=增益/减益、必殺技=必杀技 等）、文体/敬体/一致性/标点约束；call_hy3 强制要求"恰好 N 元素"JSON 数组，整批不匹配时 worker 自动**逐条降级 + 纯文本兜底**确保 100% 覆盖。多线程(THREADS=6)+ 全局最小间隔(MIN_INTERVAL=0.3) 控制速率避免 429。
- `_llm_source.json` / `_llm_translations.json` / `_llm_log.txt` 已加入 `.gitignore`。
- ⚠️ **阻断**：HY3 免费试用额度耗尽（HTTP 402, code 401008 "免费体验额度已耗尽，未开启后付费"）。约 **13260 条**已成功翻译落盘（保存在 `_llm_translations.json`，断点续译不丢）。剩余约 11641 条需用户去腾讯云控制台(tokenhub→在线推理服务)开启后付费或换新 key 才能继续；之后重跑 `python tools/llm_patch.py translate` 会自动从文件续译剩余项。

## HY3 切 hy3-preview 后续（额度再次耗尽）
- 用户要求换 `hy3-preview`（key 不变），称"申请了 100 万 token"。已将 `HY3_MODEL` 默认改为 `hy3-preview` 并在 `.env` 写入 `HY3_MODEL=hy3-preview`（key 不变）。
- 续译跑通：从 13260 续上，中途偶有 429 自动退避重试（0 失败批）。但跑到 **20079 条**后再次大面积 **402 (code 401008 免费体验额度已耗尽)**，151 个失败批被跳过，进程空转已停。
- 诊断确认：诊断最小请求仍 402，报错与之前完全相同（仍是"免费体验额度"，非"配额用尽"）——说明用户申请的 100 万 token **在该 key 上未生效**（可能未绑定 key / 未开启后付费 / 仍走免费试用池）。
- 当前状态：**已译 20079 / 剩余 4822（约 19.2 万唯一字符，主要为 freq=1 长篇散文长尾）**；已译中 1645 条译后仍含假名（质量关注，阶段二入库前需筛）。
- 注意：**每个请求都重复发送大段 system 提示词（59 术语+游戏规则，约数百 token）**，667 次请求浪费可观 token；若未来额度紧张，可大幅精简 system 提示词以省 token（剩余 4822 条即便不优化也仅需约 30 万 token，1M 额度足够）。
- 下一步：等用户在腾讯云控制台确认 100 万 token 已绑定本 key 并/或开启后付费 → 重跑 `python tools/llm_patch.py translate` 自动续译剩余 4822 条。

## 切 deepseek-v4-pro 后全量翻译完成
- 用户换 `deepseek-v4-pro`（key 不变，tokenhub 端点），已在后台启动续译剩余 4822 条。6 线程 / BATCH_SIZE=30 / MIN_INTERVAL=0.15（后续已调 0.3）。
- **翻译全量完成**：24901/24901 条 100% 覆盖写入 `_llm_translations.json`。译后仍含假名 2520 条（10.1%），其中 1978 条为 freq=1 唯一条目（散文长尾预期结果）。全站累计处理源字符约 105 万，译文字符约 83 万。
- 流程中先后三次经费额度阻断：hy3(402→停于13260)→hy3-preview(402→停于20079)→deepseek-v4-pro(跑通至结束)。`call_hy3` 的整批降级机制（逐条→`_call_plain`兜底）保证 0 丢条。
- 阶段二 `cmd_inject()` 预留，后续可将 `_llm_translations.json` 注入 `zh_patch.py` 的 JA2ZH 词表。

## 清理 LLM 翻译全部临时文件
- 翻译已全部完成，决定不再将其注入词表（仅暂时使用 LLM 加速翻译过程）。
- **已删除**：`tools/llm_patch.py`、`tools/_llm_source.json`、`tools/_llm_translations.json`、`pipeline/escah_pipeline/translator.py`、`pipeline/escah_pipeline/tm.py`、`pipeline/escah_pipeline/glossary.py`、`data/tm.json`。
- **已清理**：`.gitignore` 中 LLM 条目、`config.py` 的 LLM 配置段/TM_FILE、`updater.py` 的 LLM 提及。
- MEMORY.md 已更新移除 LLM 引用。翻译永久走 `zh_patch.py` 词表替换引擎。

## 导出未译日文 → 20 份文件供豆包翻译
- 已删 LLM 译文档无法恢复，改用豆包网页对话翻译方案：
  - `tools/_export_untranslated.py` 从 `data/parsed/zh/*.html` 提取仍含假名的标签间文本片段，按页分组导出原始 `_untranslated.txt`（19658 段, 1.75M字符）。
  - `tools/_split_untranslated.py` 将源文件按字符数均匀分割为 20 份 → `tools/_untranslated/001.txt~020.txt`（每份 66K~107K 字符）。
  - 每份文件顶部内嵌翻译提示词（术语统一规则），可直接粘贴给豆包。
  - 翻译完成后用户发回译文，由 AI 注入 `zh_patch.py` 词表后重跑生效。
  - 已完成全站残留统计：zh_patch 报告 525,602 字符残留日文。

## 重新整理翻译文本：段落级导出（准备 LLM API 翻译）
- 用户决定改用 LLM API 翻译（而非豆包），要求整合为段落级输出来改善翻译质量。
- **新建段落级导出** `tools/_export_for_api.py`：从 `data/parsed/zh/*.html` 按块级标签（p/div/li/td/h2等）提取段落，同一块内的句子不再按「。」拆分。AST 安全解析 JA2ZH(10826 key)，只输出字典未覆盖的段落。
- 产出 `tools/_for_api.txt`：**31373 段**（3.5 MB），来自 417 页面，含假名段落 31960→字典精确匹配跳过 587 段。
- ⚠️ 但发现输出含大量中文混合——因为从 zh/（词典已部分翻译）读取，残留假名的块里大部分已经是中文了。
- **已修正为 ja/zh 双文件对齐**：同时读取 ja(原文) + zh(词典后)，zh 块仍有假名时 → 输出对应的 **ja 纯日文原文**。最终导出了 **28208 段纯日文**（3.3 MB），全站点 181964 块中筛选得出。
- 新增 `tools/_split_for_api.py`：按页面整组拆分，每 batch ~4000 段，产出 **8 个 batch** → `tools/_api_batches/001~008.txt`（47~773 KB/个）。
- 新增 `tools/_api_pipeline.py`：全自动管线编排（export→split→API translate→inject→zh_patch），支持子命令 export/split/translate/inject。API 地址/key 通过环境变量 `LLM_API_URL`/`LLM_API_KEY` 配置，用户发来后直接运行。

## GLM 翻译管线修复 + 全量开跑（本次会话）
- 用户新加 GLM 三模型(glm-5/5.1/5.2，tokenhub 端点，复用 HY3 key)。实测三模型均为推理模型：默认 `content` 空、reasoning 烧 token；必须 `extra_body={"thinking":{"type":"disabled"}}` 关掉（官方文档 product/1823/132061 确认）。`_api_pipeline.py` 与 `_run_translate.py` 已加。
- 修复 `_api_pipeline.py` 多个原有 bug：`ROOT` 在 `LOG_FILE` 前未定义(NameError)、`translate_batch` 缺 `global model_index`(UnboundLocalError)；并新增模型健康探测(失败模型停用10分钟、自动改用能通的)、子批 1500 token、超时 60s、SDK `max_retries=0`，把"轮询"改成"先用完一个预算再换下一个"(glm-5→5.1→5.2)。
- 运行：`$env:PYTHONIOENCODING="utf-8"; python tools/_api_pipeline.py`(detached)。该 env 同时修复 zh_patch 子进程 Win GBK 打印日文崩溃。
- 现状：tokenhub 的 GLM 极慢(~50-80 tok/s)且常限流，glm-5/5.1 间歇性超时，基本只剩 glm-5.2 稳。已用 80 段测试批次验证端到端(译80→注入57对→zh_patch 跑通，残留日文合计 523531 字符)。全量 001-007 批(≈1.48M token)已后台开跑，按批次落盘注入 JA2ZH，可中断续跑；受真实配额限制大概率需分多轮。008 批已在前序测试译完，留 `_hold` 不重复译。

## 改为「翻译-only、注入延后」(本次会话续)
- 用户要求：本轮只翻译，注入后面做。改 `_api_pipeline.py`：`translate_batch` 去掉自动 `inject_translations` 调用（仅把译文落盘 `tools/_api_results/<批>_result.json`）；`main()` 末尾去掉自动 `run_step(zh_patch.py)`/`run_step(char_zh.py)`，改为提示事后用 `python tools/_api_pipeline.py inject` 统一注入。
- 顺手修 `_inject_batch.py` 的隐藏 bug：原来用相对路径 `tools/zh_patch.py`，CWD 不对会静默写失败（这就是之前 001 译完却没真正改 zh_patch.py 的原因）→ 改为 `Path(__file__).resolve().parent/'zh_patch.py'` 绝对路径。
- 操作：停掉仍在跑的旧进程(001 已译完且 001_result.json/001_result_pairs.json 已落盘，但 zh_patch.py git diff=0 证实旧注入静默未生效)；把 008.txt 从 _hold 放回 _api_batches(重跑会因已有结果跳过)；清理 _test 残留。以翻译-only 重启 detached，确认进程存活、有到 tokenhub 的 Established 连接、不注入。001/008 已有结果自动跳过，只译 002-007。
- 后续注入流程：`python tools/_api_pipeline.py inject`(幂等写 JA2ZH) 再 `python tools/zh_patch.py` + `python tools/char_zh.py` 应用，然后 `python -m escah_pipeline.cli sync-site` + `node site/build.mjs build`。

## 站点 UI 三项修复（MirrorContent / custom.css）
- **背景**：用户报镜像站三个问题。排查发现 `CharHoverModal.vue`+`charModalStore.ts` 功能齐全，但悬停触发**只接在 `CharList.vue`（角色一览网格）**，正文内角色链接/头像（`MirrorContent.vue` 的 v-html）完全没接线 → 浮窗"全页面不生效"。
- **修复1 表格自适应**：`custom.css` 给 `.mirror-content` 加 `overflow-x: auto`（原来 `.mirror-content table` 只有 `overflow:hidden` 圆角裁切、无横向滚动容器，宽表溢出被裁/数据展示不全）。
- **修复2 行距过大**：`.mirror-content` `line-height` 1.75→1.55；收紧 h2/h3/h4/p/ul/li 的 margin（如 h2 1.6/0.6→1.1/0.5、p 0.5→0.4、li 0.15→0.1）。
- **修复3 全页面角色浮窗**：重写 `MirrorContent.vue`，用事件委托（mouseover/click 监听 root）识别正文内 `<a class="internal-link" href="characters/名.html">`（含头像 img 内联情况），`mouseover`→`store.show(name)` 展示浮窗；`click`→`preventDefault`+`store.pinned=true` 固定浮窗（不跳转），并用 `stopPropagation` 阻断冒泡以免头像同时触发 `Layout.vue` 的图片灯箱。浮窗固定后按既有 `CharHoverModal` 头部拖动逻辑支持鼠标移动；关闭用按钮/Esc。
- 已 `node build.mjs build` 验证通过（exit 0，dist 更新）。正文角色链接形态确认：`href="characters/URL编码名.html"`（38 个 frag 含此链接）。

## 角色浮窗扩展到「名字+头像全引用」（全页面，369 角色）
- **用户新要求**：在所有 400+ 页面都实现浮窗，并「检索 369 角色的名字和头像，只要引用到的地方都绑定」。
- **数据测量**（临时脚本已删）：角色 JSON 共 **369** 个；frag 中角色名出现 **9900 次在 `<a>` 链接内**（已覆盖）+ **305 次纯文本（不在任何链接）**分布在 20 个 frag（只匹配链接会漏）；头像 img 共 5777 个，其中 **3903 个被包裹在角色链接内**（走「向上找 A」已覆盖），其余为技能图标/截图。角色 `icon` 字段形如 `attach2/<hex>.png`，其原始 URL 能 **100% 反查** `pending_assets.json` 得到正文 `/img/<hash>`（369/369 命中）。
- **新增 `tools/gen_char_refs.py`**（保留，供角色变动后重生成）：输出 `site/.vitepress/theme/charRefs.json` = `{names:[369], avatarHashes:{hash→名:369}}`。头像映射用 `https://escalationheroines.wikiru.jp/` + icon 字段直接命中 pending_assets。
- **重写 `MirrorContent.vue`**：`onMounted` 渲染后做三件事并统一打 `data-char` 标记 —— ① 角色链接 `characters/名.html`（解码名，校验在 names 集合内）；② 独立头像 img（src `/img/<hash>` 命中 avatarHashes，且祖先无 `a[data-char]` 才标，避免与链接内头像重复）；③ **纯文本角色名**（TreeWalker 收集文本节点，跳过 `a/[data-char]/script/style` 内，按长度降序正则把名字包成 `<span class="char-ref" data-char>`）。事件委托 `onOver`/`onClick` 改为 `findChar`（向上找最近 `[data-char]`）→ `store.show` / `preventDefault`+`stopPropagation`+`store.pinned=true`。纯文本匹配仅用长度≥3 的名字防误命中。
- **`custom.css` 增 `.char-ref` 样式**：非侵入（默认无样式，悬停才显虚线下划线）+ `img[data-char]{cursor:pointer}`。
- 已 `node build.mjs build` 验证通过（exit 0）。功能对全部经 `MirrorContent` 渲染的页面（即所有 wiki 内容页）生效，与 sync-site 同步的数据页数无关。⚠️ 角色增删后需重跑 `python tools/gen_char_refs.py` 再 build（否则新增角色纯文本/头像不绑定）。
- 注：frag 当前仅 49 个（即已 sync 的页面），全站 400+ 页待 sync-site 补全；浮窗逻辑本身是全站通用的。

## GLM 管线大修：001 错位污染回滚 + 多线程三模型并发（深夜续）
- **发现严重质量事故**：旧 `translate_segments` 丢弃模型返回的 `[N]` 序号按行序 append，旧 `inject_translations` 也按行序 zip——模型一漏行整批错位。001 批 3964 段只回 2184 对且大面积张冠李戴（如「閃忍ユキノ」→"你这家伙…适可而止"），且**已注入 zh_patch.py**（抽 200 对全部命中）。
- **回滚**：从 `zh_patch.py` 移除 `# ---- 001 ----` 块（2125 条错位注入，JA2ZH 回到 11001 条），备份 `zh_patch.py.bak_rollback001`；删 001 坏结果强制重翻。**008 与 _test 块验证过对齐正常（008 序号 1294/1294 连续无空洞），保留**。
- **三模型实测（2026-07-24 深夜）**：glm-5 / glm-5.1 / glm-5.2 全部可用且快（5句 1.1–3.7s），质量好、术语规则遵守。接口确实稳定了（与白天"极慢常限流"状况完全不同）。
- **重写 `_api_pipeline.py`**（覆盖式）：① 译文按 `[N]` 序号对位（漏行不错位）；② 结果文件保留全局序号有空洞不重编号，inject 按 `N→ja_list[N-1]` 配对；③ ThreadPoolExecutor 多线程（默认 6）+ 三模型 round-robin 分流；④ **每模型限速器 ModelLimiter（间隔 1.1s，tokenhub 实测每模型 RPM=60）**——6 线程裸跑会 429 风暴，加限速后 429×0；⑤ RateLimitError 退避重试不算模型故障，其他错误才停用 5 分钟；⑥ 子批 partial checkpoint（每 5 子批落盘）+ 不完整 result 自动续补 + `main()` 多轮循环（最多 3 轮回头补漏行，无增量即停）。
- **实测速率**：2 线程 ≈412 段/分；6 线程+限速 ≈**1100 段/分**、429×0（旧串行版一晚只完成 1.5 批，现在全量预计 <1 小时）。注意：长句散文批模型漏行较多（001 首轮缺 ~1200 条），靠多轮续补收敛。
- 新逻辑产出译文抽查 8/8 对齐正确。后台进程以 6 线程多轮模式运行中（跑完 001–008 全部批）。
- ⚠️ 后续注入前建议再抽查各批 pairs；注入用 `python tools/_api_pipeline.py inject`（幂等）。
